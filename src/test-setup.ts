// Vitest global test setup.
// Registers @testing-library/jest-dom matchers (toBeInTheDocument, toHaveStyle, etc.)
// and is loaded via vite.config.ts -> test.setupFiles.
import '@testing-library/jest-dom';
