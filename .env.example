# Flow Lines — environment variables (copy to .env and fill in; .env is gitignored)
# Supabase (anonymous leaderboard/scores)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Cloudflare R2 CDN base for OTA level packs (optional — bundled packs are the fallback)
VITE_CDN_BASE_URL=
