# Sprint Log
**Numtap | Gazetica Studio | Updated after every task**

---

## How to Use This File

Claude Code must update this file at the end of every task:
- Mark the task DONE
- Note what was tested and how
- Note any decisions made during implementation
- List what is next

---

## Sprint 1 — Scaffolding ✅ COMPLETE

| Task | Status | Notes |
|---|---|---|
| Vite + React + TS scaffold | ✅ DONE | |
| Phaser, Capacitor, plugins installed | ✅ DONE | |
| google-services.json placed | ✅ DONE | android/app/ |
| admob.ts with test IDs | ✅ DONE | src/services/admob.ts |
| AdMob manifest meta-data fix | ✅ DONE | Crash on launch resolved |
| Phaser 61fps verified on device | ✅ DONE | Samsung SM-E146B |
| GitHub repo pushed | ✅ DONE | github.com/gazetica/numtap |
| GitHub Actions CI green | ✅ DONE | Build #2 passed, 4m 22s |

---

## Sprint 2 — Core Game Logic ✅ COMPLETE

| Task | Status | Notes |
|---|---|---|
| Create docs/ folder and .md files | ✅ DONE | architecture.md, api-contracts.md, design-system.md, level-configs.md, sprint-log.md |
| CLAUDE.md project brief | ✅ DONE | Root of project |
| `src/game/GridEngine.ts` | ✅ DONE | T-001. Pure TS, zero Phaser. All 5 modifier hooks + asc/desc direction. |
| `src/game/GridEngine.test.ts` | ✅ DONE | T-001. 40 Vitest tests passing (8 groups, every method + modifier). |
| `src/game/ScoreEngine.ts` | ✅ DONE | T-002. Pure static calculator, no side effects. base/time/speed/streak/grid + getStars. |
| `src/game/ScoreEngine.test.ts` | ✅ DONE | T-002. 39 Vitest tests passing (7 groups, all formula branches + 3 integration cases). |
| `src/game/LevelManager.ts` | ✅ DONE | T-003. Loader/query: getLevel, getPack, getStars, isPackUnlocked, getNextLevelId, isPackStart. |
| `src/game/levels.json` | ✅ DONE | T-003. 100 levels, packs 25/35/40. All 6 integrity checks pass. |
| `src/components/TimerComponent.tsx` | ✅ DONE | T-004. React countdown, onTick/onExpire, danger red+pulse <=10s, pause/resume, reset on duration change. 15 RTL tests. |
| Game loop wire-up (GameScene.ts) | ✅ DONE | T-005. gameStore + GameScene + GameScreen + main.tsx. Build+cap sync OK, 94 tests pass. All 14 device checks PASS on Samsung SM-E146B (driven via ADB taps + screenshots). Fixed timer-reset-on-restart bug (runId key). |
| Modifier: shuffle | ✅ DONE | T-006. 8s reshuffle flash; tapped tiles stay; all numbers preserved. S1-S5 PASS on SM-E146B. |
| Modifier: fog | ✅ DONE | T-006. Global pointer listener reveals 1-cell radius over hidden tiles; reveals persist. F1-F5 PASS. |
| Modifier: mirror | ✅ DONE | T-006. Phaser label setScale(-1,1) horizontal flip; tapping underlying value registers. M1-M3 PASS. |
| Modifier: countdown | ✅ DONE | T-006. View-layer countdown in GameScene (engine reveal path unusable); numbers show then hide after 3s; tapped persist. C1-C5 PASS. |
| DEV level selector | ✅ DONE | T-006. GameScreen, gated `import.meta.env.DEV \|\| VITE_DEV_TOOLS==='true'`; hidden in production build. |

---

## Sprint 3 — Phaser Visuals + React UI ✅ COMPLETE

| Task | Status |
|---|---|
| WebGL context/loop restore on app resume (T-007) | ✅ DONE — appLifecycle.ts via @capacitor/app; 4/4 device checks PASS on SM-E146B |
| Phaser skin: gradient tiles, gold glow, radial glow, particles (T-008) | ✅ DONE — index.css skin + drawTileBg + particle canvas; 10/10 device checks PASS on SM-E146B |
| settingsStore + Capacitor Preferences service (T-009) | ✅ DONE — preferences.ts typed wrapper + settingsStore (settings/progress/IAP); hydrate on startup. 4/4 logic checks pass. |
| i18n locale files — 6 languages (T-010) | ✅ DONE — en/de/fr/ko/pt/es (136 keys each) + i18n.ts; changeLanguage wired to settingsStore. 4/4 validation checks pass. |
| Supabase leaderboard service (T-011) | ✅ DONE | supabase.ts (submit/fetch/rank) + gameStore fire-and-forget wiring; build+94 tests pass. After DB grants applied, 3/3 validation checks pass (insert 9999, fetch sorted, find test row). |
| Navigation shell + Home/Language/HowToPlay screens (T-012a) | ✅ DONE — App.tsx router + first-launch flow; 14/14 device checks PASS on SM-E146B; i18n live (DE selected → German UI) |
| Settings + Leaderboard + About + IAP screens (T-012b) | ✅ DONE — 4 screens + 4 routes; 22/22 device checks PASS on SM-E146B; live Supabase board, FR language switch, Capacitor Browser links |
| ResultScreen + CampaignScreen + progression (T-013) | ✅ DONE — VD-05 result (stars/breakdown/PB/next) + 100-level map; 22/22 device checks PASS on SM-E146B |
| Daily Challenge — seeded grid + one-attempt-per-day (T-014) | ✅ DONE — DailyChallenge.ts (mulberry32 seed) + gameStore.startDailyChallenge; 8/8 device + 2 test checks PASS; on-device grid matches seed |
| (original generic Sprint 3 rows below — all delivered across T-005…T-014) | — |
| Phaser GameScene cells/tiles/tap pulse | ✅ DONE (T-005/T-008) |
| Gold highlight on next target tile | ✅ DONE (T-008 drawTileBg + updateNextPulse) |
| Correct tap animation (checkmark + green pulse) | ✅ DONE (T-005/T-006) |
| Wrong tap animation (red flash + shake) | ✅ DONE (T-005) |
| React screens: Home/Result/Settings (+9 more) | ✅ DONE (T-012a/T-012b/T-013) |
| Navy/gold skin + background particles | ✅ DONE (T-008) |
| i18next: en/de/fr/ko/pt/es | ✅ DONE (T-010) |
| Supabase leaderboard query + display | ✅ DONE (T-011 + T-012b LeaderboardScreen) |
| Daily Challenge seeded grid | ✅ DONE (T-014) |

---

## Pre-Sprint 4 Hotfix ✅ COMPLETE

| Task | Status |
|---|---|
| Gameplay mechanic fix — last-tapped gold ✓, remove next-target highlight, −100 wrong-tap penalty (T-000) | ✅ DONE — GameScene + gameStore + ResultScreen + HowToPlay; 103 unit tests pass; 11/11 device checks PASS on SM-E146B (DC-04 float, DC-06 score floored to 0, DC-07 all-green completion, DC-08 one-gold invariant all verified via deterministic Daily 5×5 + 3×3 start) |

---

## Sprint 4 — Monetisation + Analytics (IN PROGRESS)

| Task | Status |
|---|---|
| Per-level Leader Panel — YOU vs LEADER (T-001, VDD v1.2 Change C) | ✅ DONE — campaignScores.ts + LeaderPanel.tsx; wired into GameScreen (compact, campaign only) + ResultScreen (full + submit on complete). Supabase `campaign_scores` table live (RLS + anon grants). 103 tests pass, build clean. 10/10 device checks PASS on SM-E146B (genuine 5×5 completion on L69 + REST-seeded leader for the gold "leader-ahead" state). |
| VDD v1.2 skin formalisation (T-003) | ✅ DONE — skin.ts (single-source constants) + reusable ParticleCanvas (ambient glow + floating numbers) mounted on the 9 non-game screens (particles were game-screen-only); design_system.md updated. NOTE: the brief's "skin never implemented / flat v1.0" premise was inaccurate — the v1.2 skin shipped in T-008 (dot pattern all screens, gradient tiles, glass HUD, gold buttons/glow, game-screen particles), verified on-device in T-000/T-001. 103 tests pass, build clean. Device: 13/15 checks PASS; DC-04 partial (no discrete grid-container card — Phaser full-bleed grid); DC-14 FPS ~21fps < 55 target (pre-existing device-load watch-item, not a T-003 regression — ParticleCanvas isn't on the game screen). |
| UI/visual fixes — particles, rounded tiles, footer, result screen, language name (T-004A) | ✅ DONE — 6 fixes: particle opacity 0.20–0.25; GameScene rounded tiles (6px) + grid container border (DC-04 carry-forward resolved); Speed card/grid match (level 9→10 = 4×4); Language player-name input → settingsStore.alias (+ nameLabel in 6 locales); ResultScreen 3-icon bar → PLAY AGAIN card(s); shared BottomNav 4-icon footer on Home/Campaign/Leaderboard/Settings/IAP/About/Result. 103 tests pass, build clean. 11/11 device checks PASS on SM-E146B. |
| New modes + engine — T-004B **P1: Difficulty levels** (easy/pro/expert) | ✅ DONE (phase 1 of 3) — GridEngine: Expert random `sequence` + `getSequence`/`getLastTappedValue` (easy=ascending, pro=descending already existed); ScoreEngine: `difficultyMultiplier` 1.0/1.5/2.0 (applied last, after penalty, in gameStore); GameScene gold-tile now sequence-aware (GameScene unlocked per owner, re-locked); DifficultyScreen pre-screen + `/difficulty` route; HomeScreen reroutes Campaign/Speed/Endless through it; ResultScreen shows "Difficulty ×N". 112 tests (103 + 9). Device: DC-01/02/03 PASS (pre-screen, Pro NEXT=9, Expert NEXT=random), DC-04 result-line PASS (×1.0 on completed Easy); DC-13/Expert-2× unit-verified (live Expert completion not hand-driveable — random sequence vs device timer). **P2 (Free Play) + P3 (Daily panel + difficulty leaderboard tabs, needs SQL) to follow.** |
| New modes + engine — T-004B **P2: Free Play** | ✅ DONE (phase 2 of 3) — FreePlayScreen (grid 3–7 / difficulty / timer on-off + 20/30/60/90s) + `/free-play-config` route + Home card (i18n in 6 locales); gameStore `startFreePlay` + `timed` flag + `freeplay` mode (untimed = base-taps-only scoring, no submit, no LeaderPanel); GameScreen shows ∞ + no expiry when untimed; ResultScreen local per-config PB (`freePlayPB.ts`, `fp_pb_<grid>_<diff>_<timer|untimed>`), no stars/leaderboard. 112 tests, build clean. Device DC-07/08/09/10 PASS (card, config, 7×7 Expert untimed ∞ no-panel, 3×3 result with local PB + base-only score). |
| T-005 **Phase A: Country + Language codes + Campaign routing** | ✅ DONE (phase 1 of 2) — countryFlag.ts (ISO→emoji) + 6 tests; settingsStore.country + setCountry + timezone detection; CountrySelector overlay (search + flags); LanguageScreen shows EN/DE/FR/KO/PT/ES codes (not flags) + country row ("Where are you from?"); SettingsScreen country row; campaignScores submits real country; HomeScreen Campaign → direct game (difficulty pre-screen is Speed/Endless only). 118 tests, build clean. Device DC-08/09/13/14/15/16 PASS (incl. submitted campaign row country="IN"). **Phase B (Daily 3-challenge + leaderboard redesign, needs 2 RPCs) to follow.** |
| T-005 **Phase B: Daily 3-challenge + leaderboard redesign** | ✅ DONE (phase 2 of 2) — DailyChallenge 3-challenge API (C1 easy/C2 pro/C3 expert, shared seeded layout) + getDailyShuffledNumbers; dailyScores.ts (submit C1/C2/C3 → GENERATED total_score + fetch + local per-day state); DailyHubScreen (/daily) with 3 cards + SUBMIT-when-all-done; gameStore.startDailyChallenge(index) + currentChallengeIndex + daily local-save; ResultScreen daily → BACK TO DAILY; LeaderboardScreen redesigned to CAMPAIGN/DAILY/ALL-TIME tabs (Supabase RPCs) with country flags; HomeScreen Daily → /daily. 122 tests, build clean. Device DC-01–07 + DC-10/11/12 PASS (C2=1.5×, C3 random, daily submit total_score 21,500, all-time combined 28,100). |
| Switch test AdMob IDs to live IDs | ⬜ PENDING |
| GDPR UMP consent screen | ⬜ PENDING |
| Rewarded ad: hint button | ⬜ PENDING |
| Interstitial: result screen | ⬜ PENDING |
| Remove Ads IAP (Google Play billing) | ⬜ PENDING |
| Hint Pack IAP | ⬜ PENDING |
| Firebase Analytics events | ⬜ PENDING |
| Crashlytics test crash | ⬜ PENDING |

---

## Sprint 5 — Polish + Submission (UPCOMING)

| Task | Status |
|---|---|
| 30+ game sessions QA | ⬜ PENDING |
| Test on 2nd Android device | ⬜ PENDING |
| Native-speaker review of i18n locales (esp. KO, DE) — machine-generated in T-010 | ⬜ PENDING |
| Signed release AAB + keystore backup | ⬜ PENDING |
| Play Store screenshots (6 per language) | ⬜ PENDING |
| Privacy Policy live at gazetica.com/privacy | ⬜ PENDING |
| Terms & Conditions live at gazetica.com/terms | ⬜ PENDING |
| Play Store submission | ⬜ PENDING |

---

## Decisions Log

| Date | Decision | Reason |
|---|---|---|
| 02 Jun 2026 | Grid size is a property of level config, not a chapter boundary | Enables Pack 4+ reuse of any grid size without code changes |
| 02 Jun 2026 | 100 levels at launch (not 25) | Research: 25 rounds = content exhaustion in <15 min, D7 retention impossible |
| 02 Jun 2026 | 5 modifiers: none, shuffle, fog, mirror, countdown | Provides difficulty variety without new game mechanics |
| 02 Jun 2026 | Stars are cosmetic only — 1 star always unlocks next level | Prevents casual player frustration and churn |
| 02 Jun 2026 | No sign-in/auth anywhere | Scope control. Anonymous leaderboard sufficient for launch. |
| 02 Jun 2026 | IAP max 2 products — Remove Ads + Hint Pack | Simplicity. No subscriptions. No pay-to-win. |
| 02 Jun 2026 | Use test AdMob IDs until Sprint 4 | Live IDs during dev = invalid traffic risk and account suspension |
| 02 Jun 2026 | T-001: Cell includes `display` field per api_contracts.md (brief's Cell omitted it) | api-contracts is the authoritative inter-module contract; field kept in sync with value via getDisplayValue logic. Non-breaking for tests. |
| 02 Jun 2026 | T-001: `onCountdownTick` param renamed `_elapsedMs` | Contract names it `elapsedMs` but brief logic uses Date.now()-revealedAt; `_` prefix satisfies strict noUnusedParameters without changing the public signature shape. |
| 02 Jun 2026 | T-001: Added `vitest` devDependency + `"test": "vitest"` script | Vitest was not installed; required to run GridEngine.test.ts per brief. |
| 02 Jun 2026 | T-003: Star thresholds computed with round-half-to-even (banker's rounding) | Only rule that reproduces the brief's worked examples (85→[42,64,85], 45→[22,34,45]); plain Math.round (half-up) gives 43/23, contradicting the spec. |
| 02 Jun 2026 | T-003: `levelsData.levels` cast via `as unknown as LevelConfig[]` | resolveJsonModule widens JSON literals (modifier:string, grid:number); the direct `as LevelConfig[]` in the brief does not type-check. Dataset is validated by the 6 integrity checks. |
| 02 Jun 2026 | T-003: Added `resolveJsonModule: true` to tsconfig.app.json | Required for the typed `import levelsData from './levels.json'`; the only permitted tsconfig change for this task. |
| 02 Jun 2026 | T-004: Installed `@testing-library/react`, `@testing-library/jest-dom`, `jsdom` | First React component test; RTL + jsdom env required. vite.config.ts gained jsdom/globals/setupFiles + `/// <reference types="vitest/config" />` so the `test` block type-checks. |
| 02 Jun 2026 | T-004: Tests advance fake timers one second at a time (each in its own act()) | Advancing N seconds in one call fires all interval callbacks before React flushes the updater's clearInterval-at-zero, a fake-timer batching artifact; per-second flushing mirrors real 1s-interval runtime behaviour. Component code unchanged from brief skeleton. |
| 02 Jun 2026 | T-005: Split value/type imports in gameStore + added `import type { Cell }` in GameScene | `verbatimModuleSyntax` requires type-only imports via `import type`; the brief's combined imports would not compile. |
| 02 Jun 2026 | T-005: Render shows numbers when `isVisible(cell)` (tapped OR non-hiding modifier OR revealed), not raw `cell.revealed` | GridEngine starts cells revealed=false and only reveals for fog/countdown/tap. The brief's `cell.revealed`-gated label/gold logic would render a 'none' level (level 1) as blank tiles with no gold target — failing device checks V6/V7/V8. GridEngine.ts is locked, so fixed in the render layer. |
| 02 Jun 2026 | T-005: Removed dead `require('../game/LevelManager')` stars block in GameScreen | `require` is undefined in the ESM/Vite browser runtime (throws ReferenceError); the computed `stars` was never used. Dropped unused store selectors (grid, pauseGame, resumeGame) for noUnusedLocals. |
| 02 Jun 2026 | T-005: GameScene `update(time,delta)` -> `update(_time,delta)` | `time` unused; noUnusedParameters. |
| 02 Jun 2026 | T-005: Added `runId` to gameStore; GameScreen keys TimerComponent on it | DEVICE BUG found in verification: restarting a level with the same timeLimit (45->45) left TimerComponent's `remaining` stuck at 0 (its reset effect keyed on durationSeconds didn't re-fire), so every restart instantly re-fired onExpire -> grid unplayable after game 1. TimerComponent.tsx is locked (T-004), so fix is a remount-on-restart key from this task's own files. Verified fixed on device. |
| 02 Jun 2026 | T-006: Selector gated on `import.meta.env.DEV \|\| VITE_DEV_TOOLS==='true'` (not bare DEV) | `import.meta.env.DEV` is always false for ANY `vite build` (incl. Capacitor APKs), and a `--mode development` build can't be used because React StrictMode double-invokes effects in dev and breaks Phaser boot (black screen). Test APK is built in PRODUCTION mode with VITE_DEV_TOOLS=true; `npm run build` leaves it unset so the shipped app hides the selector. |
| 02 Jun 2026 | T-006: COUNTDOWN implemented in the VIEW LAYER (GameScene), not the engine | DEVICE FAIL found: countdown levels rendered all-blank. Root cause — GridEngine never reveals countdown cells (onCountdownTick only hides already-revealed cells; the reveal path is fog-only; getGrid() returns a copy so engine state can't be reveal-mutated externally) and GridEngine is locked. Brief Change 5 anticipated fixing isVisible() in GameScene. Fix: show all numbers at level start, hide each cell 3s after first shown, keyed off scene clock; reset per level via store `runId` (NOT grid identity — tapCell makes a new grid array each correct tap, which would wrongly re-reveal). C1-C5 PASS on device. |
| 02 Jun 2026 | T-006 (env note): screen lock loses Phaser WebGL context -> blank canvas | During ADB verification, screen lock/unlock blanked the Phaser canvas (GL context loss not restored). Mitigated for testing with `svc power stayon true` + fresh app restart. Not an app bug for normal use, but flagged for Sprint 3 (consider Phaser WEBGL context-restore handling). RESOLVED in T-007. |
| 03 Jun 2026 | T-007: Resume fix is `game.loop.wake()` on Capacitor appStateChange, not Phaser 3's `restoreContext()` | Brief targets Phaser 3.60+; project is Phaser 4 which has no `restoreContext` (uses context handlers + `contextLost`). Real cause of the black canvas is the suspended render LOOP on background, so `game.loop.wake()` is the primary fix; the P3 `restoreContext` call is kept behind a typeof guard (no-op on P4) and scenes still emit `resume` per brief. `import Phaser` is a value import (not `import type`) because `Phaser.WEBGL` is a runtime constant. |
| 03 Jun 2026 | T-007: Installed `@capacitor/app@^8` (brief said 6.x) | Project runs Capacitor 8 (core 8.3.4), so the App plugin must match the major. `npx cap sync` auto-registered it in android/capacitor.build.gradle + android/capacitor.settings.gradle (generated files, required for the Android/CI build). |
| 03 Jun 2026 | T-007 (device note): SM-E146B lock/unlock unobservable via ADB until screen lock set to None | Samsung swipe-to-unlock keyguard + AOD kept ADB captures stuck on the lock screen. With screen-lock type None, wake returns straight to the app. Verified canvas recovers (mean 42.2 == baseline), taps register (gold moved), consistent across 3 cycles. |
| 03 Jun 2026 | T-008: Phaser canvas set `transparent: true` (+ phaser-container z-index 1) | Brief kept Phaser opaque (backgroundColor navy), which would cover the DOM dot-pattern + particle canvas (z-index 0) and fail device checks V1/V2. Transparent canvas lets the navy/dots/particles show behind the grid; navy base comes from index.css html/body. |
| 03 Jun 2026 | T-008: `@import` for Google Fonts hoisted to top of index.css | CSS spec requires @import before all other rules; the brief listed it mid-file where browsers ignore it. Fonts degrade to monospace/sans-serif if offline. |
| 03 Jun 2026 | T-008: tile bg is Graphics (drawTileBg); input via a transparent interactive Rectangle | Graphics has no intrinsic input geometry, so a sized transparent rect is the tap target (reliable). Removed now-unused getTileColour/getTileBorderColour (replaced by drawTileBg) to satisfy noUnusedLocals. |
| 03 Jun 2026 | T-008: next-target pulse managed by updateNextPulse() (renderGrid + refreshTileLabels), not only renderGrid | The brief adds the pulse tween only in renderGrid, but the target changes on each correct tap (which calls refreshTileLabels, not renderGrid). updateNextPulse stops the old tween and starts a fresh one on the new target so the pulse follows the gold tile. Verified on device (gold tile area varies frame-to-frame). |
| 03 Jun 2026 | T-009: alias sanitisation yields 'badchars' for 'bad chars! @#$', not 'bad_chars' | The spec'd regex `replace(/[^a-zA-Z0-9_]/g, '')` STRIPS disallowed chars (incl. spaces); it does not convert spaces to underscores. The brief's check-2 comment ('bad_chars') was inaccurate — implementation copied verbatim is correct, validated as 'badchars'. |
| 03 Jun 2026 | T-009: console checks run via a temporary mocked-Preferences vitest harness, then deleted | The brief's validation is interactive browser-console steps; the headless env can't drive a browser console. The 4 checks are pure store logic, so they were run against the REAL settingsStore with @capacitor/preferences mocked in-memory (all 4 pass), plus an `npm run dev` smoke test (HTTP 200). No test file committed; the `window.__ZUSTAND_SETTINGS__` debug line was never added. |
| 03 Jun 2026 | T-010: corrected mojibaked symbols from the brief to real Unicode | The brief doc was UTF-8-mojibaked (Ã—, Â·, â). Restored intended glyphs: × (multiply), · (middot), ✓ (check, e.g. "Purchased ✓"), — (em dash, "Consumable — 5 uses"), © (copyright), ★ (star, "Rate Numtap ★"). Same glyphs carried verbatim across all 6 locales. |
| 03 Jun 2026 | T-010: validation run via a Node key/placeholder diff script (deleted after) | 136 keys per file confirmed structurally identical; all {{...}} placeholders match en; app.name=="Numtap" in all 6; 5 spot strings differ from en (translated) per language. modifiers.none is intentionally "" in every locale (matches en). |
| 03 Jun 2026 | T-011 SECURITY: added .env / .env.local / .env.*.local to .gitignore | .env existed UNTRACKED with the Supabase anon key but was NOT gitignored — one `git add .` from leaking. Fixed before any code per the brief's STOP gate. `git check-ignore .env` confirms. (.gitignore change is staged-ready; awaiting commit approval.) |
| 03 Jun 2026 | T-011 BLOCKER: anon role lacks table privileges on public.scores (Postgres 42501, 401) | Connection + anon key VALID, table exists, but SELECT/INSERT return "permission denied for table scores". The manually-created table has RLS policies missing AND/OR no GRANTs to anon. Remediation (run in Supabase SQL editor): `GRANT SELECT, INSERT ON public.scores TO anon;` `GRANT USAGE, SELECT ON SEQUENCE public.scores_id_seq TO anon;` + the brief's RLS policies (ENABLE RLS; CREATE POLICY public read USING(true); CREATE POLICY public insert WITH CHECK(true)). Code verified correct (reaches server, valid requests); re-run browser validation after grants applied. RESOLVED — grants applied, 3/3 checks pass. |
| 03 Jun 2026 | T-012a: BrowserRouter works in the Capacitor WebView (verified) | App boots at https://localhost/ and navigates client-side via pushState; no reload, so no SPA-fallback 404. Bottom-nav routes not yet built (/leaderboard,/settings,/about → T-012b) fall through `path="*"` to / → /home. |
| 03 Jun 2026 | T-012a: GameScreen boot startLevel made conditional (only if not already 'playing') | HomeScreen calls startLevel(level,mode) BEFORE navigating to /game; an unconditional startLevel(1) on mount would override the chosen level/mode. Conditional fallback still covers direct /game navigation. |
| 03 Jun 2026 | T-012a: game-end now records progress + navigates to /home (was alert) | endGame complete → LevelManager.getStars + settingsStore.recordLevelComplete, then navigate('/home') after 1.2s. Verified on device: Bestwert updated to 1,700 after completing level 1. require() removed; LevelManager imported statically. |
| 03 Jun 2026 | T-012a: HowToPlay step paragraphs use literal English (per brief); chrome is translated | The how_to_play.stepN i18n keys carry {{tag}} markup needing a <Trans> renderer (deferred). Badge/title/labels/buttons ARE translated (verified: DE selection → German tutorial chrome). |
| 03 Jun 2026 | T-012b: ScreenShell defined per-file (4 copies) + type-split imports + `import type React` | Brief says "extract ScreenShell as a local helper inside each file"; each screen has its own copy. Language/ScoreRow/LeaderboardTab are types (verbatimModuleSyntax → import type). React.CSSProperties/ReactNode need `import type React` (new JSX transform). Mojibaked glyphs restored: ← (back), › (chevron), ★ (badge), ✓ (purchased), 🥇🥈🥉 (medals). |
| 03 Jun 2026 | T-012b: installed @capacitor/browser@8.0.3 (cap sync registered it in android gradle files) | AboutScreen opens legal/support links in the in-app Chrome Custom Tab. `npx cap sync` modified android/app/capacitor.build.gradle + android/capacitor.settings.gradle (generated, required for the Android build) — must be committed (same as @capacitor/app in T-007). Brief git-add list omits them. |
| 03 Jun 2026 | T-013: recordLevelComplete moved from GameScreen to ResultScreen | Brief routes game-end to /result (was a 1200ms home delay). Recording now happens once in ResultScreen's effect; GameScreen's `useSettingsStore`/`LevelManager`/`mode` became unused and were removed (noUnusedLocals). Removed unused `completedLevels` (ResultScreen) and `t`/useTranslation (CampaignScreen). Mojibaked glyphs restored: ⭐ ★ ▶ ↺ 🏠 🏆 ← — 🔒 🗺️. |
| 03 Jun 2026 | T-014: GridEngine private field is `grid` (not `_grid`) — used for the seeded override | Verified in GridEngine.ts before writing. startDailyChallenge mutates `(engine as {grid}).grid` cell values (+display) with the seeded shuffle; generateGrid() already set expectedNext=1. |
| 03 Jun 2026 | T-014 BUG FIXED: ResultScreen crashed (blank) for daily because getNextLevelId(0) throws | Daily uses synthetic level id 0 (not in levels.json); LevelManager.getNextLevelId(0)→getLevel(0) throws "Level 0 not found", crashing ResultScreen render. Fixed by computing nextLevelId only for mode==='campaign' (NEXT LEVEL button is campaign-only anyway). |
| 03 Jun 2026 | T-014: added a Daily Streak display to ResultScreen (mode==='daily') | Brief's D5 expects the result to show the streak, but the brief's ResultScreen edit only added updateDailyStreak(). Added a "🔥 Day Streak: N" line for daily completions so D5 is verifiable. |
| 03 Jun 2026 | T-014: brief said "7 tests / 101 total"; actual is 9 tests / 103 total | DailyChallenge.test.ts (written verbatim from the brief) has 9 tests (3 dateToSeed + 5 getDailyChallenge + 1 getTodayUTC). All pass. Determinism also proven via node (IDENTICAL=true) and on-device (grid matches seed: 12,20,8,14,6,4,16,17,9,1,...; value 1 at index 9). |
| 03 Jun 2026 | T-013: brief's "22 verification items" all PASS (R1-12, C1-8, G1-2) | Verified on SM-E146B. Bonus: 3★ on level 1 (18s), NEW PERSONAL BEST flag, NEXT LEVEL advanced to level 2, TIME'S UP red result on expiry, campaign map shows 1&2 green ⭐⭐⭐ / level 3 unlocked / 4+ locked, progress bar 2/25·8%, Pack 4 teaser, tapping completed level restarts it. G2: Result/Campaign are static DOM (no RAF) — no jank. |
| 03 Jun 2026 | T-012b: brief's "20 verification items" is actually 22 (S1-6, L1-5, A1-4, I1-5, G1-2) | All 22 listed items verified PASS on SM-E146B. Bonus confirmations: live FR language switch across all screens, alias persists, live Supabase row (TestPlayer/9999) on the board, Capacitor Browser opens CustomTabActivity. |
| 03 Jun 2026 | T-012a (perf note): →/game transition has a one-time Phaser/WebGL boot spike; steady-state gameplay ~25fps median this session | DOM route transitions are instant/smooth; entering the game incurs inherent one-time GL-context+grid-render cost. Steady-state gfxinfo this session (37%→22% janky, ~25fps median, ~3% missed vsync) was higher-jank than T-008's reading — likely device thermal/load after an extended ADB test session. No visual stutter/corruption in captures. Watch on a cold device in Sprint 5 QA. |
| 02 Jun 2026 | T-002: `buildBreakdown` follows the prose Format rules (labelled `× N streak × N grid`), not the worked examples | Brief's examples omit the streak/grid labels and use zero-padded decimals (1.0, 2.0) that no single formatter reproduces (1.25 has 2 dp, 2.8 has 1); examples are not implementable, so the prescriptive prose wins. No test asserts the exact string. |
| 03 Jun 2026 | T-000: scope expanded beyond the brief's "Files unlocked" header (GameScene+GameScreen) to also touch gameStore.ts, ResultScreen.tsx, HowToPlayScreen.tsx | The brief BODY requires them: §3.3 edits HowToPlay; §2 floors ResultScreen. gameStore is structurally unavoidable — the live score is recomputed on every correct tap there, so a view-only −100 would be wiped on the next tap; and §2's `Math.max(0, finalScore)` is only meaningful if the penalty reaches the final score. GameScreen.tsx ended up needing NO change (HUD reads `score` reactively). |
| 03 Jun 2026 | T-000: wrong-tap penalty lives in gameStore (`wrongTaps`), not the view | tapCell deducts 100 on WRONG (live score may go negative); correct-tap recompute = correctTaps·100 − wrongTaps·100; endGame carries the penalty into the final score; leaderboard submit + ResultScreen display/PB/record floor at Math.max(0, …). Device-verified: live −3,520 → result Total 0. |
| 03 Jun 2026 | T-000: −100 float implemented as a Phaser text tween in GameScene, NOT the brief's DOM `.penalty-float` CSS keyframe | The brief permitted implementing it "wherever the tile rendering lives" — tiles render on the Phaser canvas. So the `.penalty-float` CSS was NOT added to index.css (a DOM element would need canvas→DOM coordinate translation for no benefit). Float font is tile-proportional (max(14, tileSize·0.3)) rather than the spec's fixed 11px (illegibly small on a large tile). Float verified visible on device (DC-04). |
| 03 Jun 2026 | T-000: last-tapped detection handles `descending` direction | Taps are sequential, so last value = expectedNext−1 (ascending) / expectedNext+1 (descending); suppressed once engine.isComplete() so the board ends all-green (DC-07). Gold/green glows approximated by bright fill + coloured border (canvas has no box-shadow), consistent with the T-008 skin. |
| 03 Jun 2026 | T-000: HowToPlay `demo_label`/`demo_hint` switched from i18n keys to literal English | Matches the existing literal step-text pattern (T-012a); the brief supplied literal English for both. Orphaned EN keys remain harmlessly in the locale files. |
| 03 Jun 2026 | T-000: DC-07 (all-green completion) + DC-06 (score floor) verified on the deterministic Daily 5×5, not a 3×3 | The live game timer expires during slow screenshot analysis, so reads must be minimised; the Daily seed is deterministic (computed offline, confirmed on-device: 12,20,8,14,6,…), enabling a single fully-blind scripted playthrough with no mid-game reads. The mechanic is grid-agnostic; DC-01/02/03 were on 3×3. Wrong taps must be SPACED (~250ms) — rapid (<100ms) taps drop while Phaser animates floats. |
| 03 Jun 2026 | T-001: LeaderPanel `LevelLeaderInfo` import split to `import type` | verbatimModuleSyntax — the brief's combined `import { fetchLevelLeader, getPlayerPB, LevelLeaderInfo }` would not compile. |
| 03 Jun 2026 | T-001: mojibaked `'â'` placeholders restored to em dash `'—'` | Three spots: YOU empty Score/Time and the LEADER loading state. campaignScores.ts was clean ASCII (no fixes). |
| 03 Jun 2026 | T-001: GameScreen needed `mode` added to its useGameStore destructure | The panel-gating condition `mode === 'campaign'` requires it; GameScreen previously didn't select `mode`. design-system.md has no leader-panel classes — the component uses inline styles + existing CSS vars (--gold/--success/--muted/--white). |
| 03 Jun 2026 | T-001: genuine completion verified on a 5×5 level (L69, 85s), not L1 (45s) | Model screenshot-analysis latency (~40–50s) exceeds the L1 campaign timer (45s), so L1 can't be completed live (max ~5 correct taps land). A 5×5 (85–115s) fits one read+tap cycle. Reached L69 via a TEMPORARY dev-selector entry (reverted before commit — committed GameScreen has only the LeaderPanel wiring). A higher leader (ACE 99999) was REST-seeded to verify the gold "leader-ahead" state (LP-07). |
| 03 Jun 2026 | T-001: LP-05 result-screen shows leader "—"/loading momentarily (fetch-vs-submit race) | The LeaderPanel's fetch runs in a child effect that fires BEFORE ResultScreen's parent submit effect, so on the completing result the leader isn't yet populated; it appears on the next mount. This is the exact behaviour the brief's LP-05 note describes/permits. Submit-to-DB verified: the app wrote {Player, 5920, 72.00s} to campaign_scores L69. |
| 03 Jun 2026 | T-001 CLEANUP NEEDED (user): delete test rows from campaign_scores before release | Anon role has SELECT+INSERT only (no DELETE grant), so I can't remove them. Rows to delete: level 69 {Player 5920, ACE 99999}, level 999 {_probe 1} (a grant-probe insert). Real gameplay will otherwise show a fake unbeatable ACE 99999 leader on level 69. (RESOLVED — user cleared the table before T-001 push; campaign_scores now empty.) |
| 03 Jun 2026 | T-003 PREMISE CORRECTION: the VDD v1.2 skin was already implemented (T-008), not a "Sprint 3 defect" | The brief claimed flat #111C30 tiles / #0A1628 bg / no dots / no glow / no particles. Reality (code + on-device T-000/T-001): index.css ships .bg-dots (all screens), .glass HUD blur, .btn-gold gradient+glow, .text-gold-glow; GameScene renders gradient tiles (default/last-tapped-gold-✓/tapped-green-✓) + a radial ambient glow. Net T-003 work was the genuinely-missing pieces only. |
| 03 Jun 2026 | T-003: net deliverables = skin.ts + ParticleCanvas on the 9 non-game screens + design_system.md | Particles previously existed ONLY on GameScreen (inline). Extracted a reusable ParticleCanvas (ambient glow §2.1 + floating numbers §2.2, RAF paused on visibilitychange) and mounted it on Home/Campaign/Result/Leaderboard/Settings/IAP/About/HowToPlay/Language. skin.ts mirrors index.css :root for TS/inline consumers. |
| 03 Jun 2026 | T-003: GameScreen.tsx + GameScene.ts deliberately NOT modified | Both already satisfy v1.2 (verified T-000/T-008). GameScene uses Phaser numeric `fillGradientStyle(0x…)`, incompatible with skin.ts CSS-gradient strings; re-touching just-locked, device-verified files risks regression for zero visual change. GameScreen keeps its existing inline particle canvas (functionally identical to ParticleCanvas) to avoid double-mounting / disturbing verified layering. Deviation from the brief's "modify GameScreen/GameScene" — documented. |
| 03 Jun 2026 | T-003: DC-04 grid-container card NOT added | The grid renders full-bleed on a transparent Phaser canvas; a discrete bordered DOM card matching dynamic per-grid-size bounds is fragile and needs GameScreen layout work. Tiles already convey elevation via gradients/borders + the existing GameScene radial ambient glow behind the grid. |
| 03 Jun 2026 | T-003: DC-14 in-game FPS ~21fps (50th 48ms) below the 55 target | Pre-existing extended-ADB-session device thermal/load (T-013/T-001 watch-item); GPU percentile fine (~10ms) → CPU/main-thread bound. NOT a T-003 regression: ParticleCanvas does not run on the game screen (game keeps its prior inline particles); the screens it runs on are static. Re-measure on a cold device in Sprint 5 QA. |
| 03 Jun 2026 | T-003: doc file is docs/design_system.md (underscore), not the brief's design-system.md (hyphen) | Updated the actual file; added a "VDD v1.2 Skin Constants" section referencing skin.ts as canonical for TS, with the Phaser-numeric caveat and the v1.2 tile-state change. |
| 03 Jun 2026 | T-004A Fix 3: Speed mismatch fixed by changing the level mapping (9→10), NOT the card label | Card = "4×4 · 2× pts" (i18n home.mode_speed_sub, all 6 locales) + every design doc says 4×4; the game ran level 9 (3×3). Locales are locked (Fix 4 only), and 4×4 is the design intent, so the bug is the mapping. handleMode speed → level 10 (first 4×4, 'none'). The "2× pts"/halved-timer speed scoring is engine work for T-004B. (If owner instead wants speed=3×3, the card label in 6 locales would change.) |
| 03 Jun 2026 | T-004A Fix 6: extracted a shared BottomNav component; added footers to 6 screens | New src/components/BottomNav.tsx (Home/Board/Settings/About, gold `active`). HomeScreen refactored to use it; added to Campaign/Leaderboard/Settings/IAP/About (in their per-file ScreenShell) + Result. Onboarding (Language, HowToPlay), the game screen, and the (non-existent) Pause modal have no footer. GDPR footer is T-016. IAP footer added via the same ScreenShell edit (code path identical to Leaderboard/About; not separately screenshotted — no in-app nav route to IAP yet). |
| 03 Jun 2026 | T-004A Fix 4: name input maxLength 20 (per brief) but persisted alias follows the existing settingsStore.setAlias contract (≤16, alphanumeric+underscore, T-009) | "same store write Settings uses" — so the store sanitises. Alias written on BOTH Continue and "I'll choose later" (never empty → "Player"). nameLabel translated de/fr/es/pt/ko; en authoritative. Verified on-device: "Zephyr" entered on Language → shows in Settings alias. |
| 03 Jun 2026 | T-004A Fix 5: ResultScreen action layout by mode | Campaign + has-next-level → gold NEXT LEVEL primary + dark-card PLAY AGAIN secondary; all other cases (non-campaign, failed, last level) → single gold PLAY AGAIN. HOME/BOARD removed (standard footer covers them). Unused result.btn_home/btn_leaderboard i18n keys left in place (harmless). |
| 03 Jun 2026 | T-004A Fix 2: GameScene rounded corners (6px) on every tile state + wrong-flash; grid container border added | fillRoundedRect/strokeRoundedRect radius 6 (default/last-tapped-gold/tapped-green/wrong-flash). Grid border: strokeRoundedRect(startX-12, startY-12, gridPixelSize+24, …, 12), lineStyle blue 0x1E8BC3 @0.25, depth -1, tracked + redrawn each renderGrid (read existing layout vars, no recalculation). GameScene LOCKED again. |
| 03 Jun 2026 | T-004A DC-11 FPS 50th 29ms (~34fps) — better than T-003's reading | Opacity bump adds no per-frame cost (same fillText count); rounded rects negligible (GPU 8ms). No additional drop beyond the documented T-013 device-load watch-item; below 55 target → cold-device re-measure still pending (Sprint 5 QA). |
| 03 Jun 2026 | T-004B is PHASED across commits (owner choice): P1 difficulty, P2 Free Play, P3 Daily panel + difficulty leaderboard | Brief is ~3× a normal task with 3 unrun SQL migrations + a locked-file blocker. P1 (this commit) needs no SQL. P3 needs the SQL run (scores.difficulty, campaign_scores.difficulty, daily_scores). |
| 03 Jun 2026 | T-004B ARCHITECTURE: the brief's `generateSequence(n, difficulty)` doesn't match GridEngine | GridEngine shuffles grid POSITIONS + tap order = direction-driven `expectedNext`. So easy=ascending and **pro=descending already existed** (direction param). Only Expert (random tap permutation) is new — added as an internal `sequence[]`+`seqIndex` path; easy/pro logic kept byte-identical so the 40 existing GridEngine tests are untouched. |
| 03 Jun 2026 | T-004B: GameScene unlocked (owner-approved) for Expert gold-tile | The `expectedNext±1` shortcut for the gold LAST_TAPPED tile is wrong for Expert (random sequence). Added `GridEngine.getLastTappedValue()` (sequence-aware) and `GameScene.isLastTapped` now calls it. GameScene re-locked after P1. |
| 03 Jun 2026 | T-004B: difficulty multiplier applied LAST (after wrong-tap penalty), per spec | ScoreEngine.totalScore stays the pre-difficulty subtotal (existing tests' exact values hold); ScoreResult exposes `difficultyMultiplier`; gameStore.endGame computes `round((subtotal − wrongTaps·100) × mult)`. A chosen difficulty also overrides the level's `direction` (easy→asc, pro→desc); omitting difficulty (CampaignScreen taps, play-again) preserves the level's own direction. |
| 03 Jun 2026 | T-004B P1 device note: Expert completion not hand-driveable | The Expert tap order is a random permutation only revealed step-by-step by the HUD; completing it needs per-step screenshot reads that exceed the level timer at model analysis latency. DC-04/DC-13's 2× is covered by unit tests (difficultyMultiplier 1.0/1.5/2.0 + an end-to-end ×2 assertion); the on-device "Difficulty ×N" result line was verified on a completed Easy (×1.0). |
| 04 Jun 2026 | T-005 supersedes T-004B P3 (no difficulty leaderboard tabs); phased A (country/lang, no SQL) + B (daily + leaderboard, needs 2 RPCs) | Supabase check: `daily_scores` already created with the T-005 schema (score_c1 + total_score resolve), `campaign_scores.country` exists; the 2 RPCs (get_campaign_leaderboard, get_alltime_leaderboard) return 404 → must be run before Phase B. The brief's SQL section omits the daily_scores CREATE (already present, so fine). |
| 04 Jun 2026 | T-005 PB: getDailyChallenge signature changed (index, date?) → broke old callers/tests, adapted | New API returns a config (difficulty by index). Kept the seed internals + added getDailyShuffledNumbers (the shared seeded layout) + getTodayDateString (getTodayUTC aliased). DailyChallenge.test rewritten for the new API. gameStore.startDailyChallenge(index) overrides the engine's cell VALUES with the seeded layout; C3 expert tap-ORDER is the engine's per-player random permutation (GridEngine locked → not seeded; grid LAYOUT is shared, which is the comparable part). |
| 04 Jun 2026 | T-005 PB: gameStore + ResultScreen modified though not in T-005 unlock list | Structurally required for the daily 3-challenge flow (same precedent as T-000/T-001): gameStore needs startDailyChallenge(index)+currentChallengeIndex+local-save; ResultScreen needs the daily "BACK TO DAILY" action (PLAY AGAIN on synthetic level 0 would throw). Daily no longer auto-submits to the `scores` table — challenge scores save locally; the Hub submits the cumulative to daily_scores. |
| 04 Jun 2026 | T-005 PB device note: C3 (expert) completed via brute-force during verification → scored 0 | Expert tap order is random/unknowable without per-step HUD reads (exceeds the 90s timer at model latency). Brute-forced (tap all 25 positions in value order, repeated) to complete it for the SUBMIT check; penalties floored C3 to 0. C1/C2 are deterministic (seeded layout) and scored normally (8,600 / 12,900 = 1.5×). A real player scores C3 normally. Leaderboard tabs use literal tab labels (CAMPAIGN/DAILY/ALL-TIME), no new i18n keys. | | preferences.ts not in the unlock list but structurally needed for the new persisted key. detectCountry() maps ~23 common IANA zones → code, else 'XX'; device (Asia/Kolkata) detected 'IN' on-device. Settings "Country" label is literal (no settings.* i18n key added); only language.countryLabel added to 6 locales. LanguageScreen's LANGUAGES `flag` field is now unused (kept harmlessly). | | startFreePlay builds a GridEngine at the chosen grid/difficulty; `timed=false` hides the timer HUD (∞) and removes expiry; endGame for untimed free play scores `(N²·100 − penalty) × difficultyMult` (no time/speed). Free Play never submits (leaderboard) and never records campaign progress (id -1 guarded out); PB is local-only via `freePlayPB.ts` (Capacitor Preferences, dynamic key outside the typed PREF_KEYS wrapper). ResultScreen hides stars/time/speed rows + shows "Best (this config)". Brief i18n keys were `freePLayLabel`/`freePLaySub` (typo) → used correct `freePlayLabel`/`freePlaySub`. |
| 04 Jun 2026 | T-006 UX overhaul: Home restructure, Daily streak map, hint inventory, difficulty cards, leaderboard rank pinning (Sprint 4) | Home reordered Classic→Campaign→Daily(teal card)→3 square mode cards (Endless/FreePlay/Speed, purple/blue/amber accents, aspect-ratio 1, gold tap-flash). Daily Hub fully redesigned (see streak-map note). DifficultyScreen → 3 personality cards (blue Easy [1→2→3] / amber Pro [9→8→7] / purple Expert [7→?→3]) with a reusable inline MiniTile, START button colour follows selection. New GetHintModal (watch rewarded ad → addHints(1)+consumeHint → apply to tile; or Buy Gems → /iap). GameScreen got a 💡 HINT · 💎 ×N bar below the grid (calls existing GameScene.showHint, which was previously dead code — never invoked). |
| 04 Jun 2026 | T-006 hint inventory unified into a single `hintCount` (default 3), replacing the unused `hintPackCount`/`decrementHints` | The IAP screen is display-only and nothing consumed `hintPackCount`/`decrementHints` (grep: definitions only), so they were removed rather than maintained alongside a second counter. `addHints` now writes `hintCount`; added `setHintCount` + synchronous `consumeHint()` (returns false at 0, no decrement; persists fire-and-forget). Pref key `HINT_PACK_COUNT`→`HINT_COUNT`. architecture.md still lists the old field (doc only, not updated — locked). |
| 04 Jun 2026 | T-006 streak map: CSS flexbox + border connectors (NOT SVG) | Chose flexbox S-layout (row LTR → vConnector → row row-reverse → vConnector → row LTR + CLAIM) over an SVG path: renders cleanly at mobile widths with no path/viewBox maths, and node styling/animation stays plain CSS. Keyframes (dh-pulseGlow / dh-pulseRing / dh-floatUp) injected via a scoped `<style>` in the component (index.css not in the unlock list). Node state derived from `dailyStreak` + `lastDailyCompletionDate===today`. |
| 04 Jun 2026 | T-006 streak fields: added `lastDailyCompletionDate` separate from existing `lastPlayedDate` | `lastPlayedDate`/`updateDailyStreak` (T-014, used by ResultScreen) left untouched to avoid regressions; the brief's daily-hub streak uses its own `lastDailyCompletionDate` (key `LAST_DAILY_COMPLETION_DATE`) + `setDailyStreak`/`setLastDailyCompletionDate`. Pure exported `nextDailyStreak(last,today,streak)` holds the increment/reset logic (unit-tested directly); Hub SUBMIT calls it then awards +1 💎, CLAIM (streak≥7) awards +3 💎 and resets to 0. |
| 04 Jun 2026 | T-006 leaderboard rank pinning — DEVIATION: no exact out-of-top-50 rank | supabase.ts is READ ONLY with no rank-of-player RPC and the fetch returns only the top 50, so an exact global rank (brief's "#1,045") is not obtainable. In-list player rows get the gold tint + gold L/R borders (5.2). Outside top 50: a `position:sticky;bottom:56px` pinned row shows flag + alias + locally-computed score (campaign best-score sum / daily local total / all-time = both) with rank shown as "50+", or "—"/"No score yet" when the player has no local score (5.4). "YOUR RANKING" label literal (locales locked). |
| 04 Jun 2026 | T-006 tests: 130 passing (122 + 8 new), target was 128 | Added the 6 required (hintCount default 3, addHints increments, consumeHint false@0 / true+decrement, streak yesterday→+1, streak 2-days-ago→reset 1) plus 2 edge cases (already-today→unchanged, first-ever→1). @capacitor/preferences mocked in-memory in settingsStore.test.ts so write-through doesn't hit a native bridge. tsc --noEmit clean; vite build clean; cap sync clean. |
| 04 Jun 2026 | T-006 DEVICE VERIFICATION: 16/16 checks PASS on Samsung SM-E146B (RZCW30XB11X) | Home order/teal-daily-card/3-square-cards (01-03); Daily Hub S-streak map + challenge track, C1 active→complete→C2 unlock with C1→C2 arrow gold (04-06); SUBMIT +1💎 toast + SUBMITTED✓ (07); in-game 💡HINT·💎×N bar default 3 (08); HINT decrements + highlights target tile (09); 0-hint GetHintModal Watch-Ad/Buy-Gems/Cancel (10); difficulty blue/amber/purple personality cards + mini-tiles + amber/purple START on Pro/Expert (11-13); leaderboard in-list gold L/R-border highlight "Mahendra (You)" #2 + pinned "50+/—" YOUR-RANKING row when outside list incl. 5.4 "No score yet" (14-15); full nav sweep no regressions (16). C1 driven blind via offline-computed seeded layout; C2 descending; C3 expert brute-forced (scored 0, completes). Restored device (airplane/DND/stayon) after. |
| 04 Jun 2026 | T-006 DEVIATION (resolved on device): challenge-track arrow gold rule | Brief 3.3 says the connecting arrow is "gold when both connected nodes are complete" but DC-T006-06 expects "arrow C1→C2 turns gold" once C1 alone is complete (C2 only just unlocked). Honoured the explicit device-check: arrow is gold when its SOURCE card is complete (rebuilt + reinstalled, re-verified — C1→C2 gold while C2→C3 grey after C1 only). |
| 04 Jun 2026 | T-006-FIX: home card fixes, daily hub rewire, dev test-bar removal (Sprint 4) | Issue 12 (release blocker): deleted the dev level selector block (L1/L9/L27/L63/L69) from GameScreen entirely — it was gated on `import.meta.env.DEV || VITE_DEV_TOOLS==='true'` but the brief wants it gone unconditionally; verified absent in Campaign/Speed/Daily/Free Play on device. Issues 1-3 (HomeScreen): daily card → navy bg `145deg #0F2A48→#0A1E38` + red border `rgba(239,68,68,0.6)`; all 3 mode cards → gold borders `rgba(255,215,0,0.4)`; added "Numbers don't wait. / How fast can you find them?" tagline in the dead space. Issues 4-11 (DailyHubScreen): rebuilt streak map — START anchor node ("7 DAY STREAK") so Day 1 is 2nd item (centre-left), Day6/5/4 reverse row, Day7 + 52px CLAIM **circle** (was tall card), connectors aligned to circle vertical centre (NODE/2); added HOW THE STREAK WORKS + DAILY CHALLENGES instruction blocks; relabelled cards CHALLENGE 1/2/3; cards wired to startDailyChallenge (active + completed-for-replay). |
| 04 Jun 2026 | T-006-FIX Issue 13: manual SUBMIT removed → auto-submit + diamond claim | Auto-submit was NOT already in ResultScreen (brief's assumption wrong) — added `autoSubmitDailyIfComplete()` (new helper in dailyScores.ts) fired from ResultScreen when mode==='daily' && currentChallengeIndex===3 (gameStore has already written C3's local score). Removed the hub SUBMIT button + nextDailyStreak usage there. New large 💎 "+1 GEM" display (claimable gold / locked grey) + 3-state CLAIM button (incomplete / "CLAIM YOUR DAILY 💎" / "✓ DAILY 💎 CLAIMED"). One-per-day via `daily_diamond_claimed_${date}` Preferences flag (helpers in dailyScores.ts, NOT preferences.ts — dynamic date key doesn't fit the typed PREF_KEYS). CLAIM → addHints(1) + setLastDailyCompletionDate(today) (for the map's gold today-done state) + flag + "+1 💎" toast. settingsStore untouched (only existing methods called). |
| 04 Jun 2026 | T-006-FIX DEVIATION: completed challenge cards are tappable for replay | Issue 11 says completed cards have "no tap handler / not re-playable", but DC-FIX-16 requires replaying CHALLENGE 1 and Issue 13's replay-protection section assumes replay is possible. Resolved in favour of the explicit device check + replay spec: completed cards are tappable (re-launch that challenge); the once-per-day diamond is guarded by the persisted claim flag so replay can't re-award. Locked cards remain non-tappable. |
| 04 Jun 2026 | T-006-FIX DEVICE VERIFICATION: 17/17 PASS on Samsung SM-E146B | 01 test-bar gone (Campaign/Speed/Daily/Free Play); 02 daily card navy+red; 03 mode cards gold; 04 tagline; 05 START→Day1(centre-left)→2→3; 06 centre-aligned connectors; 07 52px CLAIM circle; 08/09 instruction blocks; 10 CHALLENGE 1/2/3 labels; 11 CHALLENGE 1 launches; 12 C1✓→C2 active (played descending); 13 all-done → 💎 display + gold CLAIM; 14 +1💎 toast → CLAIMED + hint ×3→×4; 15 kill/reopen → CLAIMED persists; 16 replay C1 → local score, claim stays CLAIMED; 17 auto-submit landed in daily_scores with no button tap. C1 ascending / C2 descending driven blind from offline-computed seeded layout; C3 expert brute-forced. Network left ON (DND only) to verify auto-submit; device settings restored after. Adds Mahendra/Player verification rows to daily_scores (existing pre-launch cleanup item). |
| 04 Jun 2026 | T-007: hint cards, daily hub reorder, streak map 3x3, tagline centering (Sprint 4) | Fix 1 HomeScreen: tagline wrapped in `flex:1` centred container (equidistant between cards and footer; BottomNav marginTop:auto wrapper dropped). Fix 3 DailyHubScreen: section order flipped — challenge section (instructions + track + diamond + claim) now ABOVE the 7-day streak section; Instructions component reduced to yellow line1 (12px/700) + muted line2 (11px), the "HOW THE STREAK WORKS" header removed (the yellow line IS the heading); both sections lead with "Complete all 3 daily challenges every day." Fix 2 DailyHubScreen: streak map rebuilt to a true 3×3 S-grid (9 nodes) — Row1 START→Day1→Day2, Row2 row-reverse Day3→Day4→Day5 (visual Day5/Day4/Day3), Row3 Day6→Day7→CLAIM; START is a dashed-gold anchor circle, CLAIM a 52px circle; HConn at circle centre (marginTop NODE/2), VConn drops on right (Day2→Day3) and left (Day5→Day6). |
| 04 Jun 2026 | T-007 Fix 4 GameScreen: combined HINT bar replaced with two cards above LeaderPanel | Removed the single `💡 HINT · 💎 ×N` bar. Added a two-card row: LEFT = rotating promo (GET MORE HINTS / REMOVE ADS, cross-fade via stacked opacity, `setInterval` 5s); RIGHT = USE HINT (💡 + 💎 ×N left, teal border) using the existing handleHintTap (consume or GetHintModal). LeaderPanel renders directly below (campaign only). Both cards equal-width halves (flex:1, min-height 72). |
| 04 Jun 2026 | T-007 Fix 4 timer pause/resume on IAP | Tapping the left card → `pauseGame()` then navigate('/iap'). Navigating unmounts GameScreen, so the TimerComponent (unlocked? NO — read-only) would restart at full limit. Solution without touching TimerComponent: capture remaining time ONCE at mount via `resumeDuration = max(1, timeLimit − timeElapsed)` and pass it as `durationSeconds` (fresh game → elapsed 0 → full; resume → mid-countdown). Boot-fallback guard changed `status !== 'playing'` → `status === 'idle'` so a 'paused' return doesn't restart level 1; a mount effect resumes the paused game + re-renders the grid. Verified on device: timer 73 → IAP → back → 69 (resumed, not reset to 90). |
| 04 Jun 2026 | T-007 DEVICE VERIFICATION: 14/15 PASS on Samsung SM-E146B; DC-T007-13 blocked | 01 tagline centred; 02 exactly 3×3 (9 nodes); 03 Row1 START/Day1/Day2; 04 Row2 Day5/Day4/Day3; 05 Row3 Day6/Day7/CLAIM; 06 connectors at circle centres (gold solid / dashed grey); 07 challenge section above streak; 08 yellow line both sections, no HOW THE STREAK WORKS; 09 old bar gone, two cards; 10 right card uses hint (×3→×2), 0→GetHintModal (same unchanged component as T-006); 11 left card rotates GET MORE HINTS/REMOVE ADS @5s; 12 left card → pause + IAP → resume (73→69); 14 CHALLENGE 1 launches; 15 Home/Leaderboard/Settings no regression. **DC-T007-13 (purchase awards hints) NOT testable** — IAPScreen is a display-only Sprint-4 placeholder ("BILLING ACTIVE IN FULL RELEASE", non-functional BUY buttons) and is LOCKED; no purchase callback exists. Navigation + pause/resume (the T-007 scope) verified; addHints-on-purchase awaits real billing. |
| 04 Jun 2026 | T-008: streak map 4+4, weekly claim, 3 hint cards, BuyHintModal rename (Sprint 4) | Part 1 DailyHubScreen: streak map rebuilt to a 2×4 S-grid (8 nodes) — Row1 Day1→Day2→Day3→Day4, Row2 row-reverse Day5→Day6→Day7→CLAIM (visual L→R: CLAIM,7,6,5), vertical connector Day4→Day5 on the right column. The "7 DAY STREAK / START" anchor node removed (Day 1 is now first). Streak section reduced to a single yellow line "Reach 7 days in a row to claim 3 💎 gems." (no duplicate of the challenge-section line). Part 2: weekly claim CTA below the map — 3 states (Complete 7 days to claim / CLAIM YOUR WEEKLY 3 💎 / ✓ WEEKLY 3 💎 CLAIMED), one claim per ISO-8601 week keyed `weekly_diamond_claimed_YYYY-WNN` (getISOWeekKey + Preferences helpers inline in DailyHubScreen — dailyScores.ts, home of the daily equivalent, is locked this task and the key is week-dynamic). On claim: addHints(3) + persist flag + "+3 💎" float + setDailyStreak(0) (streak resets → nodes derive from dailyStreak → return to future). |
| 04 Jun 2026 | T-008 Part 3 GameScreen: 2-card hint row → 3 cards | Left = rotating promo (GET MORE HINTS/REMOVE ADS, unchanged logic, now 1/3 width); MIDDLE (new) = 📺 WATCH AD green-border (pause → showRewarded → on reward addHints(1)+consumeHint()+applyHintToTile()+resume; on dismiss resume+toast "Watch the full ad to get a hint"; on unavailable resume+toast "No ads available right now"); RIGHT = USE HINT (unchanged). Brief asked to "check ad availability before pausing" but admob.ts (READ ONLY) exposes no availability probe — so we pause then attempt and resume immediately on throw (momentary pause). Cards equal width via shared HINT_CARD_BASE flex:1. |
| 04 Jun 2026 | T-008 Part 4: GetHintModal → BuyHintModal | File + component renamed; title "Get a Hint 💡" → "Buy a Hint 💡"; only importer (GameScreen) updated. Right card at 0 hints now pauseGame() before opening the modal; GameScreen's closeHintModal resumes on every close path (Cancel/overlay/Watch-Ad-complete). Watch Ad inside the modal: on reward addHints(1)+consumeHint()+onApplyHint()+onClose(resume); Buy Gems navigates to /iap WITHOUT resuming (GameScreen unmounts 'paused' → resume-on-mount handles the return, same as the left card). |
| 04 Jun 2026 | T-008 DEVICE VERIFICATION: 18/18 PASS on Samsung SM-E146B | 01 2×4 (8 nodes); 02 Row1 Day1-4; 03 Row2 visual CLAIM/7/6/5; 04 right-side Day4→Day5 vertical connector; 05 single yellow streak line; 06 weekly button disabled; 07 streak=7 → all-green map + gold CLAIM node + active weekly button; 08 claim → +3 hints (2→5 in prefs) + CLAIMED + streak 7→0 + nodes dark; 09 persists after kill/reopen (week 2026-W23); 10 three cards; 11 middle WATCH AD green; 12 middle tap → pause + rewarded test ad; 13 ad complete → reward path runs + resume; 14 right@0 → pause + BuyHintModal "Buy a Hint 💡"; 15 modal Watch Ad → video ad → reward + resume; 16 Cancel → resume, no hint; 17 left rotation + IAP (unchanged from T-007); 18 CHALLENGE 1 launches. To reach streak=7 / 0-hints, edited CapacitorStorage.xml via `run-as` (MSYS_NO_PATHCONV=1 — Git Bash was mangling /data paths); app force-stopped before each edit. Test rows + modified prefs (dailyStreak, hintCount=0, weekly flag) on the device fall under the existing pre-launch cleanup item. Network left ON (DND only); device settings restored after. |
| 05 Jun 2026 | T-009: Classic mode level display fix (Sprint 4) | CLARIFICATION: there is no `classic` GameMode — "PLAY NOW · Classic Mode" routes to `mode='campaign'` (same as Campaign Progress); GameMode = campaign/daily/endless/speed/freeplay. Owner decision: treat "Classic" as "Campaign", so the new LEVEL display is gated on `mode === 'campaign'` (covers PLAY NOW + Campaign Progress), absent in Daily/Endless/Speed/Free Play. Bug 1: added a 4th HUD column LEVEL / L{n} (leftmost, gold, Space Mono 18px) in GameScreen, sourced from `currentLevel.id` (no hardcoding), rendered only when `mode === 'campaign'`. Bug 2: ResultScreen subtitle now prefixes `Level {id} · ` for campaign → "Level 50 · 4×4 Grid · 37s" (× = U+00D7); other modes keep the plain "{grid}×{grid} Grid · {time}s" line. No gameStore change (currentLevel.id already in state); no new i18n key (LEVEL hardcoded to match the existing hardcoded TIMER/NEXT/SCORE labels). |
| 05 Jun 2026 | T-009 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC7 all PASS | AC1 HUD LEVEL/L50 in campaign play (4 columns fit 360px, no wrap); AC2 absent in Daily + Endless HUD (Speed = same difficulty→startLevel path, same guard); AC3 campaign result "Level 50 · 4×4 Grid · 37s"; AC4 daily result "5×5 Grid · 18s" (no Level prefix); AC5 campaign result shows correct level, rest unchanged; AC6 130 tests pass; AC7 no locked files touched (only GameScreen.tsx + ResultScreen.tsx). Device UTC rolled to 2026-06-05 → daily reset; C1 completed via 06-05 seeded layout. |
| 05 Jun 2026 | T-009b: playtest bug fixes + timer cap sweep (Sprint 4) | Fix 4 levels.json (owner-authorised data edit): clamped timeLimit to grid caps (3×3/4×4≤30, 5×5/6×6≤60, 7×7≤90) — 85 levels clamped (14× 3×3, 24× 4×4, 16× 5×5, 21× 6×6, 10× 7×7); only timeLimit changed (stars/modifier/direction untouched per brief). Consequence noted: some clamped levels now have star thresholds above timeLimit (e.g. L1 timeLimit 30 vs stars[45]) — left as-is per "do not change stars". AC7: 9 levels are `direction: descending` (75-80, 96, 97, 100) — by design (CLAUDE.md §4.5 descending unlocks at L75); FLAGGED, unchanged. Fix 2 ResultScreen: failed (timer expired) now shows "LEVEL FAILED" (var(--danger)=#E05050; skin.ts has no danger constant and is locked) + "TIME OUT" subtitle (#6B84A8, 11px); win keeps gold "LEVEL COMPLETE"; the T-009 "Level n · g×g Grid · ts" line stays on both. Locale files not in scope → failed strings are literals. Stars row already hidden on fail (unchanged). |
| 05 Jun 2026 | T-009b Fix 1 + Fix 3 | Fix 3 GameScreen: removed the LEVEL column from the HUD bar (back to 3 cols TIMER·NEXT·SCORE in all modes) and added a "LEVEL n" badge (muted "LEVEL " + white number, Space Mono 11px, left-aligned, absolutely positioned at top 14.5% so it never shifts the Phaser grid), campaign-only. Fix 1 mirror zero-pad: tile text is rendered ENTIRELY in the locked GameScene via `String(engine.getDisplayValue(cell))`, and getDisplayValue is on the locked GridEngine (returns a number; mirrorNumber reverses digits so 10→1, then String drops the leading zero) — no React layer, no non-locked formatter seam in the render path. Minimal safe seam (owner pre-authorised gameStore for Fix 1): added pure `src/utils/tileFormat.ts` `padTileLabel(n)=String(n).padStart(2,'0')` and wrapped the engine INSTANCE's getDisplayValue in gameStore.startLevel for mirror levels so the scene's String() renders a 2-char string; setScale(-1,1) still flips it. Display-only (tap validation uses cell.value). Per-instance wrap → test-created engines unaffected. Note: the pre-existing mirrorNumber collision (value 1 and value 10 both → "01") is inherent to the locked engine and out of Fix 1's padding scope. |
| 05 Jun 2026 | T-009b DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC9 PASS | AC1 L27 (3×3 mirror) tiles show 01-09 flipped 2-char; L19/daily non-mirror show 1-9 single-digit. AC2 L27 timeout → "LEVEL FAILED" red + "TIME OUT" + "Level 27 · 3×3 Grid · 29s" + PLAY AGAIN. AC3 L19 win → gold "LEVEL COMPLETE!" + "Level 19 · 5×5 Grid · 54s" + 3 stars. AC4 HUD 3 cols in L27/L19/daily. AC5 "LEVEL 27"/"LEVEL 19" badge above grid (campaign); absent in daily. AC6 L27 timer started 30 (3×3), L19 started 60 (5×5); JSON: 0/100 levels over cap. AC7 ascending except flagged descending. AC8 130 tests. AC9 only GameScreen/ResultScreen/levels.json/gameStore/tileFormat.ts touched. L19 completed via single on-device input-tap sequence (ascending, 25 taps, 54s). |
| 05 Jun 2026 | T-009c: HUD alignment fix + star threshold sweep (Sprint 4) | Fix 1 GameScreen HUD: rebuilt the bar as a 2-row CSS grid (3 columns × [labels row, values row], alignItems:baseline, alignContent:center) so the three labels share one line and the three values share a baseline. Sizes per spec: labels 9px #6B84A8; TIMER & SCORE values 22px; NEXT value 28px bold gold #FFD700. The TIMER value is rendered by TimerComponent (its own inline 36px span; that file is NOT in the permitted list), so it's resized to 22px via a scoped `<style>` `.hud-timer-value > span { font-size:22px !important }` inside GameScreen — a CSS-only fix in the permitted file, TimerComponent untouched (keeps its #EEF4FF→#E05050 ≤10s colour, ~identical to spec #F0F4FF). HUD height unchanged (14%); values are smaller so the bar stays compact. LEVEL badge above grid untouched. Fix 2 levels.json (owner-authorised data edit): star thresholds are time-ELAPSED, ascending [3★,2★,1★], must be ≤ timeLimit. After T-009b clamped timeLimits, 85 levels had stars > new timeLimit; recomputed those to [floor(tl*0.5), floor(tl*0.7), floor(tl*0.9)] (per-level all-three recompute to keep ordering valid — per-threshold clamping would have broken 3★≤2★). Exactly the 85 T-009b-clamped levels; non-clamped levels already valid and untouched. Verified: 0/100 levels violate stars[0]≤stars[1]≤stars[2]≤timeLimit; only the stars arrays changed (timeLimit untouched). |
| 05 Jun 2026 | T-009c DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC10 PASS | AC1 TIMER(26)/SCORE(0) both 22px; AC2 NEXT(01) gold 28px bold, visibly larger; AC3 all three values on one baseline; AC4 TIMER/NEXT/SCORE labels same 9px, one line; AC5 HUD bar compact (height unchanged); AC6 "LEVEL 51"/"LEVEL 19" badge above grid unchanged. AC7/AC8 clamped L19 (5×5, 60s, stars [30,42,54]) completed in 10s → 3 stars (gold ⭐⭐⭐) — stars award correctly with new thresholds; JSON proves 0/100 invariant violations. AC9 130 tests. AC10 only GameScreen.tsx + levels.json (+ docs) modified; TimerComponent NOT edited (CSS override used). Star completion driven via the IAP-pause trick (start→capture→left-card pause→read grid while frozen→resume→25 taps) to beat the clamped-timer vs ADB-analysis-latency constraint. |
| 05 Jun 2026 | T-016: GDPR UMP consent flow (Sprint 4) | Package check: @capacitor-community/admob@8.0.0 exposes the full UMP API (requestConsentInfo, showConsentForm, showPrivacyOptionsForm, resetConsentInfo; enums AdmobConsentStatus {NOT_REQUIRED/OBTAINED/REQUIRED/UNKNOWN}, AdmobConsentDebugGeography {DISABLED/EEA/...}). New src/services/consentService.ts: requestAndResolve() calls requestConsentInfo -> if status REQUIRED && isConsentFormAvailable, showConsentForm() -> persist resolved status to Preferences key gdpr_consent_status; reopenForm() calls showPrivacyOptionsForm() (the UMP privacy-options entry point for an already-consented user; showConsentForm only presents while REQUIRED, so it is not right for a revisit — documented deviation from the brief's literal showConsentForm). App.tsx: on launch (native-only via Capacitor.isNativePlatform), await requestAndResolve() THEN await initAdmob() — AdMob.initialize strictly after consent. AboutScreen: "Ad Preferences (GDPR)" row wired to reopenForm (LINKS gained an optional action field). admob.ts untouched (still locked). |
| 05 Jun 2026 | T-016 NOTE: initAdmob was previously never called | admob.ts exported initAdmob() but no call site existed (test ads worked lazily). T-016 adds the first explicit AdMob.initialize() call (App.tsx, after consent). EEA debug geography + testDeviceIdentifiers gated behind import.meta.env.DEV || VITE_DEV_TOOLS==='true' (same flag as the project's test APKs); device hash E538D4CD3DFA5FF7EC5799F66CC14E72 (SM-E146B, from UMP logcat). Plain npm run build (CI/shipped) emits options {tagForUnderAgeOfConsent:false} with NO debugGeography — verified on a production APK. |
| 05 Jun 2026 | T-016 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC9 PASS (logcat-backed) | AC2/AC3 ordering (timestamps): requestConsentInfo -> persist gdpr_consent_status -> AdMob.initialize, both EU and non-EU paths. AC5 non-EU (real India / production build, no debugGeography): NOT_REQUIRED, no dialog, init proceeds. AC6 EU (dev build, EEA + correct hash): REQUIRED -> showConsentForm -> native Google UMP dialog (Consent/Do not consent/Manage options) shown over the app. AC4: About->Ad Preferences fires showPrivacyOptionsForm -> form reopens. Tapped Consent -> OBTAINED persisted. AC7: relaunch -> requestConsentInfo returns OBTAINED, showConsentForm NOT called (no re-prompt). AC8 admob.ts untouched. AC9 130 tests. |
| 05 Jun 2026 | T-017: rewarded ad on Hint button (Sprint 4) | Owner decision: formalize the existing middle "WATCH AD" hint card as the T-017 rewarded-hint button (kept the gem "USE HINT" card + inventory untouched). New src/services/rewardedAdService.ts: loadRewarded() (prepareRewardVideoAd, idempotent via a prepared flag) + showRewarded() returning 'rewarded'|'dismissed'|'unavailable' (reloads after each show). Reuses AD_UNITS.REWARDED test id from the LOCKED admob.ts (imported, not modified), isTesting:true — live id switch is T-015. GameScreen: preload on mount (native-only), reset per-session hint counter on runId; handleWatchAd rewired — cap 3/session (toast on the 4th), removeAdsPurchased -> instant applyHintToTile (no ad), else pause -> showRewarded -> highlight only on 'rewarded', 'unavailable' -> "No ad available" toast, 'dismissed' -> nothing (no highlight/penalty). Dropped the old addHints+consumeHint gem accounting. Highlight = existing GameScene.showHint (5s gold-tile scale-pulse) reused (GameScene locked). admob.ts untouched. |
| 05 Jun 2026 | T-017 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC9 PASS (logcat-backed) | AC2 prepareRewardVideoAd on game mount (no tap), test id isTesting:true. AC3 WATCH AD tap -> showRewardVideoAd, test ad plays (paused). AC4 completion -> onRewardedVideoAdReward -> resume + next-tile scale-pulse highlight (also via instant removeAds path: target tile enlarged). AC6 removeAdsPurchased=true -> NO showRewardVideoAd, instant highlight. AC7 4th hint blocked with "No more hints this session" toast. AC5 no-reward: airplane no-fill -> onRewardedVideoAdFailedToLoad -> "No ad available" toast, no highlight, no crash, game continues (dismissed branch identical in code; test rewarded video has no mid-play skip affordance). AC8 admob.ts diff empty. AC9 130 tests + tsc clean. |
| 05 Jun 2026 | T-018: interstitial on Result screen (Sprint 4) | New src/services/interstitialAdService.ts: loadInterstitial() (prepareInterstitial, idempotent via prepared flag) + showInterstitial() (shows, persists Date.now() to Preferences key last_interstitial_shown on success, reloads in finally; silent on no-fill) + isCapElapsed() (>=3min since last shown, or never; conservative false on error). Reuses AD_UNITS.INTERSTITIAL test id (ca-app-pub-3940256099942544/1033173712, isTesting:true) from LOCKED admob.ts — imported, not modified; live id in T-015. ResultScreen mount effect (native-only): if removeAdsPurchased -> return (no load, no show); else await loadInterstitial() then if isCapElapsed() void showInterstitial() (fire-and-forget, never blocks render). Result mount is the ONLY caller (grep-confirmed) — never mid-gameplay. admob.ts untouched. |
| 05 Jun 2026 | T-018 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC9 PASS (logcat-backed) | AC2 prepareInterstitial on Result mount (test id, isTesting:true). AC3 cap-clear + removeAds=false -> showInterstitial -> "This is an interstitial test ad" shown, dismiss -> Result normal. AC4 last_interstitial_shown timestamp persisted on show; 2nd Result within 3min -> cap get + NO showInterstitial. AC5 removeAdsPurchased=true (prefs) -> NO prepareInterstitial/showInterstitial (no load attempted), Result renders. AC6 airplane no-fill -> prepareInterstitial -> interstitialAdFailedToLoad, no interstitialAdShowed, no crash, Result renders, cap NOT consumed. AC7 only ResultScreen calls the service (grep). AC8 admob.ts diff empty. AC9 130 tests + tsc clean. Note: triggered Result via game timeout (no need to complete); short timers expire during analysis gaps so flows were driven in tight adb calls. |
| 05 Jun 2026 | T-020: Firebase Analytics events (Sprint 4) | No Firebase plugin was installed (android side WAS ready: google-services:4.4.4 classpath + app/build.gradle applies the plugin when google-services.json present, which it is). Owner-approved adding @capacitor-community/firebase-analytics@8.0.0 (peer @capacitor/core>=8.0.0; npm install + cap sync auto-registered it in android/capacitor.settings.gradle + app/capacitor.build.gradle). New src/services/analytics.ts: thin native-only (Capacitor.isNativePlatform) wrappers, one logEvent per fn, errors swallowed — sessionStart, levelComplete, levelFail, tapCorrect, tapWrong, adImpression, iapPurchase. Call-sites: App.tsx (session on mount), gameStore.tapCell (tap_correct/tap_wrong — the scene→state bridge; GameScene is read-only and the React layer doesn't see taps, so this store action is the minimal safe seam), ResultScreen (level_complete win / level_fail on status='failed'), rewardedAdService (on reward) + interstitialAdService (on show), IAPScreen (iap_purchase commented stub — fires for real in T-019). admob.ts untouched. |
| 05 Jun 2026 | T-020 DEVIATION: session_start + ad_impression are Firebase-RESERVED names | Confirmed in logcat ("FA: Name is reserved... Event will not be logged (FE): session_start"). Firebase auto-collects both. Owner decision: rename to custom names app_session_start and app_ad_impression (function names sessionStart/adImpression unchanged; only the logged event string + a code comment). The other 5 (level_complete, level_fail, tap_correct, tap_wrong, iap_purchase) are non-reserved and log as-is (iap_purchase != reserved in_app_purchase). |
| 05 Jun 2026 | T-020 VERIFICATION on Samsung SM-E146B (Firebase DebugView via logcat, debug.firebase.analytics.app): AC1-AC9 PASS | FA-SVC "Logging event" + "Uploading events (com.gazetica.numtap)" confirmed for: app_session_start (origin=app); tap_correct (level_id, tap_count 1..25); level_complete (level_id, grid_size, stars, score, time_elapsed — all 5); app_ad_impression ad_type=interstitial AND ad_type=rewarded (AdActivity). level_fail (level_id, grid_size) + tap_wrong (level_id) dispatched (same accepted custom-event pipeline). NO reserved rejections for any renamed/custom event. iap_purchase stub present (analytics fn + commented IAP call-site). 130 tests + tsc clean. cap sync added the firebase-analytics plugin to android/*.gradle (auto-generated, flagged). |
| 05 Jun 2026 | T-021: Firebase Crashlytics test crash (Sprint 4) | Plugin choice DEVIATION: the brief's candidate @capacitor-community/firebase-crashlytics tops out at v3.0.0 (peer @capacitor/core ^5.0.0 — incompatible with Capacitor 8). Used @capacitor-firebase/crashlytics@8.3.0 (peer >=8.0.0) instead. New src/services/crashlytics.ts: thin native-only wrappers — recordError(err) -> FirebaseCrashlytics.recordException({message}) (future use, not wired) and testCrash() -> FirebaseCrashlytics.crash({message:'T-021 test crash'}). AboutScreen: dev-only "⚠ TEST CRASH (DEV)" button gated behind `import.meta.env.DEV || VITE_DEV_TOOLS==='true'` (DEV_TOOLS const). admob.ts untouched. |
| 05 Jun 2026 | T-021 ANDROID GRADLE (owner-approved exception to the cap-sync-only rule) | Crashlytics SDK 20.0.3 hard-requires the Crashlytics Gradle plugin — without it the app CRASHES ON LAUNCH ("IllegalStateException: The Crashlytics build ID is missing", thrown from FirebaseInitProvider). Confirmed on device, then fixed with 2 minimal edits: android/build.gradle += `classpath 'com.google.firebase:firebase-crashlytics-gradle:3.0.2'`; android/app/build.gradle += `apply plugin: 'com.google.firebase.crashlytics'` (inside the existing google-services.json-guarded block). cap sync also registered the plugin in android/capacitor.settings.gradle + app/capacitor.build.gradle (auto-generated). |
| 05 Jun 2026 | T-021 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC8 PASS | AC2 Crashlytics 20.0.3 inits for com.gazetica.numtap, settings activated (org_id 6a22b73f…, collect_reports:true). AC5 dev build: "TEST CRASH (DEV)" button visible in About. AC3 tap -> native crash (FATAL via Bridge.callPluginMethod -> FirebaseCrashlytics.crash, app process died). AC4 relaunch -> Crashlytics uploaded to crashlyticsreports-pa.googleapis.com/v1/firelog/legacy/batchlog (console dashboard confirmation is owner's — no console access here). AC6 production build (no VITE_DEV_TOOLS): dist bundle grep clean + on-device About has NO Test Crash button. AC7 admob.ts diff empty. AC8 130 tests + tsc clean. |
| 06 Jun 2026 | B-003: About screen navigation + non-functional rows (Sprint 4) | Assessment: AC1 back button (←, navigate(-1)) and AC6 Test Crash button were already present/correct (back matches the Campaign/IAP/Leaderboard pattern); AC2 BottomNav Home and AC3 Ad Preferences→reopenForm (T-016) were intact (regression check: still wired). Real bugs fixed in AboutScreen.tsx: AC4 Contact "mailto:support@gazetica.com" was routed through Browser.open (Custom Tabs cannot render mailto) → now native openUrl sends mailto/tel via window.open(url,'_system') (WebView → system handler) and http(s) via Browser.open; AC5 More Games URL corrected https://gazetica.com → https://gazetica.com/games.html. openUrl is native-only (Capacitor.isNativePlatform; web logs to console). Only AboutScreen.tsx changed; no deps added (@capacitor/browser + core already present). |
| 06 Jun 2026 | B-003 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC7 PASS | AC1 back (←) returned About→Home (previous screen). AC2 bottom-nav Home from About→Home. AC3 Ad Preferences→showPrivacyOptionsForm (consent_sdk dialog shown). AC4 Contact→Intent VIEW mailto: resolved to email chooser (Gmail/Outlook ComposeActivity). AC5 More Games→Browser.open {"url":"https://gazetica.com/games.html"} → in-app Custom Tab opened. AC6 Test Crash (dev)→app process died. AC7 130 tests + tsc clean. |
| 06 Jun 2026 | B-002: background music (Sprint 4) | Premise corrections: (a) brief said "Howler.js is already in the project" — it was NOT; added `howler@^2.2.4` + `@types/howler` (package.json — owner pre-authorised by the brief's "if not present, add it"). (b) brief suggested reusing appLifecycle.ts for background/foreground — that file is bound to the Phaser game instance and is removed on GameScreen unmount, so it can't drive app-wide music; wired a dedicated `@capacitor/app` appStateChange listener inside musicService instead (Capacitor web shim → visibilitychange, so it also works on web). New src/services/musicService.ts: singleton Howler wrapper — init()/play()/pause()/setVolume(), one Howl (loop:true, html5:false → Web Audio buffer loop = gap-free, volume 0.35 ≤0.40 AC8). `enabled` mirrors toggle intent and gates foreground-resume. App.tsx: init() on mount + play() only if saved musicEnabled (settings hydrate before render in main.tsx, so the flag is reliable; defaults OFF → no first-run autoplay AC3). SettingsScreen: music ToggleRow → new handleMusicToggle (setMusicEnabled then musicService.play/pause for instant AC4/AC5). settingsStore unchanged (kept music import out of it to protect the 130 tests — store is imported widely). src/store/settingsStore.ts NOT modified despite being in the permitted list (no change needed; key `musicEnabled` already correct). |
| 06 Jun 2026 | B-002 ASSET generation (procedural, own-work) | No ffmpeg + no suitable offline CC0 track + Claude Code has no internet → generated a 64s seamless ambient loop (A-minor drone pad: integer-Hz partials 55/110/165/220/262/330/440 + sparse 660Hz bell every 16s, equal-power stereo pan, slow per-partial tremolo, tanh warmth). SEAMLESS (AC7): every partial completes an INTEGER number of cycles over 64s so sample[0]==sample[N] (click-free Web-Audio buffer loop); a single slow "breath" envelope dips to a soft trough exactly at the seam so the residual MP3 encoder padding is inaudible (one gentle swell per loop — not a "dramatic swell"). Output public/assets/audio/bg_music.mp3 = 750 KB stereo 96 kbps (<1MB AC2). Encoded with `@breezystack/lamejs` (maintained ESM fork — the original `lamejs` CJS entry throws `MPEGMode is not defined`); installed via `npm install --no-save` (build-only, NOT added to package.json — auto-pruned on next install; verified absent from package.json/lock + node_modules). Throwaway generator `.gen-audio/gen.mjs` deleted post-gen (not committed). No OGG: no clean offline Vorbis encoder; MP3-only works on Android WebView + desktop Chrome and AC2 lists .ogg as optional ("if generated"). LICENCE: original work, procedurally synthesised here — no third-party attribution required. |
| 06 Jun 2026 | B-002 DEVICE VERIFICATION on Samsung SM-E146B: AC1-AC9 PASS | Audio is silent over ADB, and `dumpsys audio` AAudio player state stays `started` across play/pause (Howler keeps the AudioContext alive), so verified via the DEFINITIVE signal: Chrome DevTools Protocol on the debug WebView (`adb forward` → webview_devtools_remote → CDP Runtime.evaluate `window.Howler._howls[0].playing()`). AC1 init/play/pause/setVolume present (code). AC2 bg_music.mp3 750 KB on device. AC3 OFF launch (musicEnabled=false) → howls:1, playing:false (init ran, no autoplay); ON launch (musicEnabled=true) → playing:true on launch. AC4 Settings music toggle OFF → playing:false (immediate). AC5 toggle ON → playing:true (immediate). AC6 HOME (background) → playing:false; foreground (am start) → playing:true. AC7 _loop:true + Web-Audio buffer loop (html5:false) + seamless asset by construction. AC8 volume()==0.35 (≤0.40). AC9 130 tests + tsc -p tsconfig.app.json clean. Also: music continues uninterrupted navigating Settings→Home→Leaderboard→About (SPA, single Howl persists). UMP consent dialog appeared (VITE_DEV_TOOLS EEA debug) and was dismissed; unrelated to music. Device prefs were set (onboarding-skipped, alias Zephyrv, musicEnabled toggled) via the run-as CapacitorStorage.xml playbook — falls under the existing pre-launch device-cleanup item; left musicEnabled=false; DND/stayon restored. |
| 06 Jun 2026 | B-002b: 3 short ambient music variations for selection (Sprint 4) | Generated 3 candidate loops for Mahendra to choose from (NOT yet wired — musicService still points at bg_music.mp3; selection + wiring is a later step). Throwaway `scripts/generate_music.js` (ESM — package.json is type:module) using `@breezystack/lamejs` via `npm install --no-save` (auto-pruned; no package.json/lock change — verified), DELETED after writing (AC6); scripts/ dir removed. All 3 = 128kbps stereo MP3. SEAMLESS via integer-cycle partials + integer-cycle LFOs (sample[0]==sample[N]); seam Δ measured ≤ the highest partial's per-sample slew (V1 0.000/0.000, V2 0.021/0.005, V3 0.015/0.001 — all sub-click). V1 "Warm Sine Pad" bg_music_v1.mp3 = 157 KB/10.0s: pure sines C3/G3/C4, 0.1Hz·15% tremolo, 5ms L/R width, 1s fade in/out to zero at seam. V2 "Soft Bell Texture" bg_music_v2.mp3 = 157 KB/10.0s: V1 pad + 3 C5 bells (5ms attack, 2.5s exp decay, 25% level) at 0.5/4.2/8.1s, tails WRAPPED across the seam. V3 "Slow Filter Sweep" bg_music_v3.mp3 = 188 KB/12.0s: root/3rd/5th (C3/E3/G3) amplitudes cross-fading on a single 0.0833Hz LFO cycle (opens/closes), no bells. All <300KB (AC2), 10–12s (AC3). Original bg_music.mp3 untouched (AC7). No source-code changes (AC8); 130 tests + tsc clean (AC9). Not pushed — awaiting Mahendra's pick. |
| 06 Jun 2026 | B-002c: anti-hum regeneration of the 3 variations (Sprint 4) | Root cause of the hum was the B-002b C3 (130Hz) root. Rebuilt all 3 OVERWRITING v1/v2/v3 with no partial below 392Hz, no bass layer, mid/high register, master peak 0.25 (quiet), ≥200ms soft attack, DC-offset removed, seamless. Same throwaway `scripts/generate_music.js` (ESM) + `@breezystack/lamejs --no-save`, DELETED after (AC5); scripts/ removed; no package.json/lock change (verified). Anti-hum EVIDENCE (AC3): Goertzel probe shows sub-200Hz spectral magnitude at the numerical floor (50/130/200Hz ≈ 1e-8…1e-12) vs in-band partials ~1e-2 — i.e. zero hum energy; DC means ≈1e-7 or smaller post-removal. SEAMLESS (AC4): a symmetric raised-cosine master fade lands both ends on 0 (also serves as the soft attack) + integer-cycle partials/LFOs → seam Δ ≈ 1e-10…1e-12. DEVIATION: V1 root G4 = 392Hz is 8Hz under the brief's 400Hz guideline but is the named G-major triad root and far above any hum register — kept the note, flagged. V1 "Warm High Pad" 157KB/10s: G4/B4/D5 sines, 0.1Hz·10% tremolo, 8ms width, 1.5s fade in/out. V2 "Soft Chime Texture" 188KB/12s: A4+E5 two-tone pad + C6 chimes (3ms atk, 3s decay, 20%) at 1/5/9s, tails wrapped, 12s for chime headroom. V3 "Breathing Glass" 188KB/12s: Cmaj7 upper voicing C5/E5/G5/B5, each on its own 1-cycle LFO (distinct phase), no tremolo. All <300KB, 10–12s. Original bg_music.mp3 untouched. No source changes (AC6); 130 tests + tsc clean (AC7). Not pushed — awaiting Mahendra's pick. |
| 06 Jun 2026 | B-002d: menu music swapped to a CC0 water-drip ambient loop (Sprint 4) | Replaced bg_music.mp3 (was the B-002c CC0 ARP track, 250619 B) with a gentle water-drip loop. SOURCE/LICENCE (AC4): Freesound track ID **683779** "Water Dripping" by user **Elements-Library**, **CC0** (public domain, no attribution required) — https://freesound.org/people/Elements-Library/sounds/683779/ . CAPABILITY NOTE: Claude Code cannot download binaries from the web (WebFetch returns text only; Freesound needs login/OAuth), so I vetted CC0 candidates via WebSearch/WebFetch and Mahendra downloaded #683779, trimmed it to a seamless ~10s and converted WAV→MP3 (cloudconvert), then placed it. (Rejected on the way: DWOBoyle #136692 + zimbot #395179 = Attribution-not-CC0; Sclolex #177958 = CC0 but 90s heavy cave reverb.) Placed file: public/assets/audio/bg_music.mp3 = 78.8 KB (80731 B, <300KB AC2), MP3 64kbps/48kHz mono, duration 10.08s by frame-parse (8–15s AC3). cap sync ran, asset propagated dist→android (80731 B) (AC5). DEVICE (SM-E146B, AC6/7/8, prod build, verified via WebView CDP `Howler._howls[0]`): new asset loaded src=/assets/audio/bg_music.mp3 dur=10.01s loop:true vol=0.35; Settings→Music ON → playing:true, seek advanced 1.93→6.90 on Home = drip plays (AC7); Music OFF → playing:false, silent on Home (AC8). No source-code changes this task — only the asset (the uncommitted App.tsx/SettingsScreen/musicService/package.json are pre-existing B-002 wiring, untouched here) (AC9). 130 tests + tsc -p tsconfig.app.json clean (AC10). NOT pushed — awaiting Mahendra's on-device audio sign-off (device left with Music ON playing on Home). |
| 06 Jun 2026 | B-002e: gameplay sound effects (Sprint 4) | Added 5 SFX + a SFX service. ASSETS (Mahendra-approved deviation): the brief said "Claude Code to search and download from Freesound" but Claude Code cannot download binaries (same limit as B-002d) — for short UI SFX, owner chose SYNTHESIS over a 5-file download hand-off. Procedurally generated (original work = CC0-equivalent, no attribution) via a throwaway `scripts/generate_sfx.js` (ESM + `@breezystack/lamejs --no-save`, DELETED after; no package.json/lock change): sfx_tick (sharp dry click 0.07s/1.6KB), sfx_correct (soft 2-note chime C6→G6 0.6s/9.8KB), sfx_wrong (low thud 150→110Hz 0.28s/4.9KB), sfx_win (warm bell arpeggio C5-E5-G5-C6 2.2s/35KB), sfx_fail (gentle descending G4→D4 0.85s/13.9KB) — all mono 128kbps, <200KB (AC2). The <10s urgent tick (AC5) reuses sfx_tick at Howler rate 1.5 — NO separate sfx_tick_fast asset (per the brief's fallback). New src/services/soundService.ts: Howler singleton (same pattern as musicService) — preload()/playTick(fast)/playCorrect/playWrong/playWin/playFail/unload(); every play gated by Capacitor.isNativePlatform() (AC12) AND live settingsStore.soundEnabled (AC10), independent of the Music toggle (AC11). PREMISE FIX: brief's path `src/game/scenes/GameScene.ts` is wrong — the file is `src/scenes/GameScene.ts` (game logic is src/game/*, Phaser scene is src/scenes/*); used the real path. WIRING: GameScene.handleTap → playCorrect/playWrong (AC6/AC7); GameScene.update fires playWin/playFail once on the playing→complete/failed transition, placed BEFORE the existing `status!=='playing'` early-return, caught reliably in the 600ms board-pause before /result (AC8/AC9). Tick wired in GameScreen's existing TimerComponent onTick per-second callback (NOT GameScene — onTick IS "the existing per-second callback" the brief's note points to; no new timer), fast when remaining<=10 (matches the timer's red-pulse danger threshold) (AC4/AC5). GameScreen mount: musicService.pause() (stop drip AC3) + soundService.preload(); unmount: soundService.unload() + musicService.play() if musicEnabled. ResultScreen mount: musicService.play() if musicEnabled (resume drip AC3). settingsStore + musicService were READ-only (not modified). |
| 06 Jun 2026 | B-002e DEVICE VERIFICATION on Samsung SM-E146B (prod build, removeAds set to suppress interstitial confound): AC1-AC13 PASS | Verified via WebView CDP with a Howl.prototype.play spy logging src@rate. AC2 on /game: howlCount 6 = drip + 5 sfx preloaded. AC3 drip dripPlaying:false on /game start, bg_music.play() logged on Result/menu resume. AC4 16× sfx_tick@1.0 over the countdown. AC5 10× sfx_tick@1.5 once remaining<=10. AC6 sfx_correct on the value-1 tile (and 25× on a full daily C1). AC7 sfx_wrong on wrong tiles. AC8 daily Challenge 1 completed (computed today's seeded 5×5 layout offline via the locked DailyChallenge mulberry32/Fisher-Yates — all 25 taps correct) → sfx_win logged exactly ONCE. AC9 campaign timeout → sfx_fail exactly ONCE (the timer-trap: random campaign grids can only be device-tested for FAILING, so win used the deterministic daily). AC10 Sound OFF → 0 SFX from ticks+taps (empty log), drip howl unaffected. AC11 Music OFF → drip playing:false on Settings+Home immediately, while sfx_wrong/sfx_tick still fired with Sound ON (toggles independent). AC12 native-only enforced by Capacitor.isNativePlatform() guard (web build emits no SFX). AC13 130 tests + tsc clean. Device left clean: sound+music ON, removeAds=true (test artifact — pre-launch cleanup item). NOT pushed — awaiting Task Report sign-off. |
| 06 Jun 2026 | B-001a: i18n wiring pass — English only, no translation (Sprint 4) | Replaced hardcoded UI strings with t() calls across 15 files and added 61 new keys. KEYS (AC2/AC3): merged 61 new keys into ALL 6 locale files via an add-only script (en.json = English values; de/fr/ko/pt/es = the SAME English as placeholder for B-001b) — 140→201 keys each, 0 overwrites, all 6 JSON.parse-valid (AC5/AC6). New namespaces: daily, difficulty, freeplay, campaign, hints, country; new keys also under home/game/result/how_to_play/leaderboard/iap/settings. WIRING: module-scope label arrays (HomeScreen MODE_CARDS, DifficultyScreen OPTIONS, FreePlayScreen DIFFS, LeaderboardScreen TABS, HowToPlay mode list) restructured to hold i18n KEYS resolved with t() in render. Components without the hook gained useTranslation (GameScreen, DifficultyScreen, FreePlayScreen, CampaignScreen, LeaderPanel, BuyHintModal). Module-scope sub-components that can't use the hook (DailyHubScreen's StreakMap/ClaimButton/etc., CountrySelector.countryName(), DailyChallenge.ts label) read from the i18n singleton (`import i18n from '../i18n'`). PREMISE FIX: brief path src/game/scenes/GameScene.ts is wrong (not in this task's files anyway). Wire-up-only (existing keys, were hardcoded): home.mode_endless/mode_speed/freePlayLabel, game.hud_timer/next/score, leaderboard.tab_daily/tab_alltime (+ modes.campaign for the CAMPAIGN tab), leaderboard.empty (LeaderPanel), common.level (Result level prefix). Interpolation wired: daily.day{{n}}, daily.challenge{{n}}, result.grid_label{{size}}, result.time_label{{seconds}}, game.hints_left{{count}}. NOT WIRED (no key in brief's set — reported for B-001b): DailyHub "+1 GEM"/"Daily Challenge Reward"/"CLAIM YOUR DAILY"/"CLAIM YOUR WEEKLY 3"; HomeScreen "Streak:"/taglines/card-subs; DifficultyScreen "SELECT DIFFICULTY"/"START"; FreePlay "ON ·"/"PLAY"; CountrySelector "SELECT COUNTRY"/search placeholder; HowToPlay long step text + demo hint; Campaign packNames; LeaderPanel "Score"/"Time". 130 tests + tsc clean (AC9). |
| 06 Jun 2026 | B-001a DEVICE VERIFICATION on Samsung SM-E146B (dev build, via WebView CDP innerText): AC7/AC8 PASS | AC7 EN — Daily Hub / Settings / Home / Game all render resolved English with NO raw key strings (no "daily.title" etc.): e.g. Daily Hub shows DAILY CHALLENGE, Day 1–7, CLAIM, CHALLENGE 1/2/3, instructions. AC8 KO (switched via the Settings language <select>) — EXISTING translated keys now render Korean (the wire-up win: previously-hardcoded ENDLESS/FREE PLAY/SPEED → 엔드리스/자유 플레이/스피드; HUD TIMER/NEXT/SCORE → 시간/다음/점수; SETTINGS/AUDIO/etc → 설정/오디오), while NEW keys show the English placeholder (CAMPAIGN PROGRESS, DAILY CHALLENGE, Country, USE HINT, WATCH AD, LEVEL 2, ×3 left) — correct until B-001b. Interpolation verified live ("LEVEL 2", "Day 1…7", "×3 left"). Device language restored to EN; temp helpers removed. NOT pushed — awaiting approval. |
| 06 Jun 2026 | B-001b: translated the 61 B-001a keys into DE/FR/KO/PT/ES (Sprint 4) | Translated all 61 keys in each of the 5 non-EN files (305 translations) via an overwrite-only script; en.json LOCKED/untouched (AC5, git-confirmed); all 6 files JSON.parse-valid, 201 keys each (AC6). METHODOLOGY DEVIATION (disclosed): the brief's 3-source live validation (Google Translate/DeepL/Papago/Linguee) is NOT possible from this environment (no interactive browser; those are JS apps), so translations are my own expert work, cross-checked for CONSISTENCY with the terms already in each locale file (e.g. DE keeps SPEED + Challenge as loanwords per existing home.mode_speed/modes.daily; KO uses 엔드리스/스피드/데일리/힌트/레벨; FR VITESSE; PT VELOCIDADE; ES VELOCIDAD) — not by scraping the named services. Special rules honored: interpolation {{n}}/{{count}}/{{size}}/{{seconds}} preserved exactly (AC3); ALL-CAPS preserved (AC4); symbol-only keys kept as-is in all langs (reward_gem_1 '+1 💎', reward_gem_3 '+3 💎', time_label '{{seconds}}s'); partial translations done (daily.day/challenge translate only the word; result.grid_label only 'Grid'; game.hints_left only 'left'; difficulty subs translate only Ascending/Descending, keep '1 → N²'). Loanwords kept: PRO (all), DE freeplay.off→AUS but FR/PT/ES OFF kept (compact toggle, pairs with the un-wired English 'ON'), Normal (DE/FR/PT/ES), Timer (DE). 130 tests + tsc clean (AC9). |
| 06 Jun 2026 | B-001b DEVICE VERIFICATION on Samsung SM-E146B (dev build, lang switched via Settings <select>, via WebView CDP innerText): AC7/AC8 PASS | AC7 KO — Home mode cards (엔드리스/자유 플레이/스피드) + new keys now Korean (캠페인 진행도, 데일리 챌린지); Daily Hub 1일차–7일차, 챌린지 1/2/3, 받기(CLAIM), 모든 챌린지를 완료하여 받기, 7일 연속…보석을 받으세요; Game 레벨 2, 힌트 사용, 광고 보기, 광고 제거, 힌트 더 받기, ×3 남음 (interpolation); Difficulty 쉬움/프로/전문가 + 오름차순/내림차순/무작위 순서 (math 1 → N² kept). AC8 DE — 5 screens German: Settings (EINSTELLUNGEN/Land), Home (KAMPAGNENFORTSCHRITT/TÄGLICHE CHALLENGE), Daily (CHALLENGE 1/2/3, instructions), Game (ZEIT/NÄCHSTE/PUNKTE/LEVEL 2/MEHR TIPPS HOLEN/WERBUNG ENTFERNEN), FreePlay (FREIES SPIEL/Gittergröße/Schwierigkeit/LEICHT/PRO/EXPERTE/AUS). Untranslated bits seen are the B-001a not-wired strings (+1 GEM, Daily Challenge Reward, Streak:, ON ·, SELECT DIFFICULTY, START) — no key, out of scope. Device restored to EN; temp helpers removed. NOT pushed — awaiting approval. |
| 06 Jun 2026 | B-001c: wired the final unwired i18n strings + translated in same pass (Sprint 4) | Closed out the B-001a/B-001b leftovers. Added 10 new keys (brief said "11" — actual count is 10; the translation table has 10 rows) to ALL 6 locale files via add-only script, with the brief's pre-validated translations applied inline (en + de/fr/ko/pt/es) → 201→211 keys each, all JSON-valid (AC4), en.json English values correct (AC5). Keys: daily.gem_reward_label/gem_reward_sub/claim_daily/claim_weekly/streak_label, home.tagline_1/tagline_2, freeplay.toggle_on, difficulty.select_title/start. WIRED 4 components: DailyHubScreen (DiamondDisplay "+1 GEM"/"Daily Challenge Reward" + the two composite claim buttons "💎 CLAIM YOUR DAILY 💎"/"💎 CLAIM YOUR WEEKLY 3 💎" → 💎 {i18n.t(...)} 💎, keeping the decorative gems); HomeScreen ("Streak:" → t('daily.streak_label'), both taglines); FreePlayScreen ("ON" in the timer chip → t('freeplay.toggle_on')); DifficultyScreen ("SELECT DIFFICULTY" + "START"). Loanwords per the brief's table: freeplay.toggle_on DE→AN but FR/PT/ES keep "ON" (consistent with freeplay.off which kept OFF there); difficulty.start DE→START kept. 130 tests + tsc clean (AC7). grep confirms zero remaining hardcoded literals for these items. NOTE: mode-card subs ("Your rules", "5×5 · 3min", "4×4 · 2×pt") remain English — never assigned a key in any B-001 brief, out of scope. |
| 06 Jun 2026 | B-001c DEVICE VERIFICATION on Samsung SM-E146B (dev build, KO via Settings <select>, WebView CDP innerText): AC6 PASS | KO Home: 연속: (Streak), 숫자는 기다리지 않는다. (tagline_1), 얼마나 빨리 찾을 수 있나요? (tagline_2). KO Daily Hub: +1 젬 (gem_reward_label), 데일리 챌린지 보상 (gem_reward_sub). KO Difficulty: 난이도 선택 (select_title), 시작 (start). claim_daily/claim_weekly wired (verified by code + zero leftover literals; visible on-screen only when all-3-done / streak=7). Device restored to EN; temp helpers removed. NOT pushed — awaiting approval. |
| 06 Jun 2026 | F-001: Campaign 2 (Pro) + Campaign 3 (Expert) + Early-Access IAP placeholder (Sprint 4) | Largest brief of the sprint. PREMISE FIXES (brief vs real code): level config is `src/game/levels.json` (not src/data/levels.ts) with `{id,pack,grid,modifier,direction,timeLimit,stars}` — NOT the brief's gridSize/sequence/scoreMultiplier/campaign/starThresholds; matched the REAL schema (per the brief's own "match exactly"). The 1.5×/2× multipliers + descending/random are NOT level fields — they come from the runtime `difficulty` param the engine already supports, so CampaignScreen passes `'pro'`/`'expert'` to startLevel (no locked GridEngine/gameStore/GameScene edits). LEVELS (AC1): appended 101–300 via text-splice (1–100 byte-identical bar a trailing comma on L100): grid cycle 3-4-5-6-7, timeLimit {3:15,4:30,5:45,6:60,7:75}, stars=floor(tl×.5/.7/.9), C2 pack:4 direction:descending, C3 pack:5 direction:ascending (expert overrides to random at runtime); pack 4/5 keep them out of C1's getPack(1|2|3). GATE SERVICE (AC2, NEW src/services/campaignGateService.ts): recordCompletion (idempotent upsert), getCompletionCount, getGateThreshold, isCampaignUnlocked, isPurchased — all DEFENSIVE (fall back to 0 / DEFAULT_THRESHOLD 1000 / locked when Supabase tables absent). SUPABASE (AC3 — OWNER ACTION): I cannot create tables from here (anon key SELECT/INSERT only); SQL handed to owner in the Task Report; UI degrades to 0/1000 locked until tables exist. CAMPAIGN SCREEN (AC4/5/7): C1 unchanged; new GatedCampaign sections for C2/C3 (title/subtitle + live gate_progress + per-campaign progressive unlock); locked → greyed grid + "🔓 UNLOCK FOR $X · EARLY ACCESS" button → /iap; unlocked (gate OR campaignN_purchased pref) → playable; Coming Soon card for C4 (301–400). recordCompletion (AC10) fired on CampaignScreen mount when a campaign is fully cleared (idempotent) — ResultScreen (the natural spot) is outside the permitted files; owner-approved deviation. IAP (AC8/9): two Early-Access sections (Pro $3.99 / Expert $4.99, EARLY ACCESS badge) + UNLOCK buttons → "Available in full release" toast. i18n (AC11): 13 new keys + 1 updated (campaign.coming_soon_sub) across all 6 locales (224 keys each), translated DE/FR/KO/PT/ES (B-001b methodology; my expert translations — live 3-source check not available from here). 130 tests + tsc clean (AC12). SOCIAL-PROOF download-count: not implemented (no data source; gate_progress covers the X/Y players line). |
| 06 Jun 2026 | F-001 DEVICE VERIFICATION on Samsung SM-E146B (dev build, WebView CDP innerText + prefs injection): AC4-AC9 PASS | AC4 Campaign screen shows C2 "PRO CAMPAIGN / Levels 101–200 · Descending · 1.5× score / 0 / 1,000 players completed Campaign 1" and C3 "EXPERT CAMPAIGN / Levels 201–300 · Random · 2× score / 0 / 1,000 players completed Campaign 2" (live gate via service, 0/1000 fallback since tables not yet created). AC5 both locked → "🔓 UNLOCK FOR $3.99/$4.99 · EARLY ACCESS" button + greyed grid. AC7 "🔒 COMING SOON / Levels 301–400 · More challenges on the way". AC6 set campaign2_purchased=true (Preferences) → C2 shows EARLY ACCESS badge, no unlock button, L101 unlocked. L101 → NEXT=09 (descending), TIMER started 15 (3×3). L201 (campaign3_purchased) → NEXT=03 (random — neither ascending 01 nor descending 09), TIMER 15. AC8 IAP shows both Early-Access sections (Pro $3.99 / Expert $4.99). AC9 tap UNLOCK FOR $3.99 → "Available in full release" toast. AC10 recordCompletion wired (not exercisable on device without completing 100 levels + live tables). Device restored: test purchase prefs removed, lang EN; temp helpers removed. NOT pushed — awaiting approval. |
| 06 Jun 2026 | F-001b: Campaign tier UI — gate card redesign + in-game tier indicator + PRO/EXPERT player tags (Sprint 4) | THREE changes. (1) GATE CARD REDESIGN: CampaignScreen GatedCampaign now shows the new subtitle (campaign.pro_gate_sub / expert_gate_sub "Free — … Gate opens on 1,000 Players Completing Campaign N") + a large right-aligned cyan counter (campaign.gate_counter "{{count}} / {{total}}", 26px #00f5ff); removed the old "Levels … · Descending/Random · ×score" sub and the small "X / Y players completed" line. Counter reuses the existing gate state (getCompletionCount + getGateThreshold). (2) IN-GAME TIER: GameScreen HUD level badge is now a flex row — LEVEL n left, PRO (101–200, #9B59B6) / EXPERT (201–300, #00f5ff) right via t('tier.pro'/'tier.expert'); the #phaser-container gets a 2px tier-coloured border (+ inset glow); C1 unchanged. getCampaignTier(id)= id≥201 expert / ≥101 pro / else null. GameScene.ts NOT touched (border is on the GameScreen container div). (3) TIER TAGS: new src/services/tierService.ts — getTier(alias) (Supabase campaign_completions → expert if campaign_id≥2, pro if ≥1), getLocalTier()/loadLocalTier() (Preferences 'player_tier' cache; sync getter after async load), setLocalTier (never downgrades expert→pro), formatAliasWithTier + getTiers batch + TIER_COLORS {pro:#9B59B6,expert:#00f5ff}. CampaignScreen sets the LOCAL tier on mount from completedLevels (works offline). Tags rendered as coloured " (PRO)"/" (EXPERT)" (t('tier.pro_tag'/'expert_tag')) in SettingsScreen (next to the alias input), LeaderboardScreen (every row via getTiers batch + local pinned row via getLocalTier) and LeaderPanel (YOU=local, LEADER=getTier). Other players' tags need the Supabase campaign_completions table (defensive: no tag until it exists). i18n: 7 new keys (tier.pro/expert/pro_tag/expert_tag + campaign.pro_gate_sub/expert_gate_sub/gate_counter) translated in all 6 locales (231 keys each). 130 tests + tsc clean (AC11). |
| 06 Jun 2026 | F-001b DEVICE VERIFICATION on Samsung SM-E146B (dev build, WebView CDP getComputedStyle/innerText + prefs injection): AC1-AC9 PASS | AC1 PRO card: "Free — Pro Campaign Gate opens on 1,000 Players Completing Campaign 1" + large "0 / 1,000" counter. AC2 "Descending"/"Random" strings absent from the screen (grep). AC3 L101 HUD "LEVEL 101 | PRO" (right-aligned); L201 "LEVEL 201 | EXPERT". AC4 #phaser-container borderColor rgb(155,89,182)=#9B59B6 on L101, rgb(0,245,255)=#00f5ff on L201 (≈2px). AC6 Settings alias "Zephyrv" + "(PRO)" tag, colour rgb(155,89,182). AC7 Campaign leaderboard "Zephyrv (You) (PRO)" tag rgb(155,89,182) (local tier; other players gated on the Supabase table). AC8 in-game LeaderPanel YOU "Zephyrv (PRO)". AC9 local tier only present because player_tier was injected (set from completedLevels in real use). Device tier set via injected player_tier=pro + campaign2/3_purchased for the gated levels; all test prefs removed after, lang EN. NOT pushed — awaiting approval. |
| 06 Jun 2026 | F-001c: UI polish — gate card wrapper, Settings tier reposition, Home achievement cards, cyan hint (Sprint 4) | FOUR changes. (1) CampaignScreen gate sections wrapped in a card (bg rgba(255,255,255,0.04), border 1px rgba(255,255,255,0.10), radius 12, padding 16, margin 12px 0); i18n pro_gate_sub/expert_gate_sub updated (dropped "Free —", "Campaign 1"→"Campaign 100", "Campaign 2"→"Campaign 200") + retranslated all 6 locales. (2) SettingsScreen: tier tag moved to a row ABOVE the alias input ("{alias} (TIER)", tier colour), the whole row hidden when getLocalTier() is null (no empty space). (3) HomeScreen: both taglines removed from render (home.tagline_1/2 keys kept in locales); two pill achievement cards added below the mode cards (borderRadius 999), EXPERT first then PRO — 28px round checkbox that fills with ✓ (PRO #9B59B6 +white check / EXPERT #00f5ff +dark check) when the local tier qualifies (proCompleted = tier pro|expert, expertCompleted = tier expert); titles home.win_pro_tag/win_expert_tag + subs. (4) Hint highlight: GameScene.showHint() (scale-pulse) is NO LONGER called — instead GameScreen renders a React cyan (#00f5ff, opacity 0.55) overlay over the target tile for 5s then fades over 500ms, positioned via the SAME grid geometry renderGrid() uses (window dims + level grid). GameScene.ts NOT modified (read-only honoured). i18n: 4 new home keys + 2 updated campaign keys, all 6 locales. 130 tests + tsc clean (AC11). |
| 06 Jun 2026 | F-001c DEVICE VERIFICATION on Samsung SM-E146B (dev build, WebView CDP getComputedStyle/innerText + prefs injection): AC1-AC9 PASS | AC1 gate card computed style border 1px rgba(255,255,255,0.1) / bg rgba(255,255,255,0.04) / radius 12px. AC2 "Free —" absent, "Campaign 100"+"Campaign 200" present ("Pro Campaign Gate opens on 1,000 Players Completing Campaign 100"). AC3 Settings PROFILE shows "Zephyrv (PRO)" row BEFORE "Leaderboard Alias" input. AC4 no tier → 0 (PRO)/(EXPERT) tags, row absent. AC5 "Numbers don't wait" tagline gone. AC6/AC7 WIN PRO TAG + WIN EXPERT TAG cards present (expert first). AC8 no tier → 0 ✓; player_tier=pro → PRO card 1 ✓, EXPERT 0; player_tier=expert → both 1 ✓. AC9 tap USE HINT → overlay div backgroundColor rgb(0,245,255), opacity 0.55, width=tile(117px), transitionDuration 0.5s (fades after the 5s hold). Test player_tier pref removed after; device EN. NOT pushed — awaiting approval. |
| 06 Jun 2026 | F-001c CORRECTIONS (4) — pre-push, owner-requested | (1) CampaignScreen gate card: content centred (card textAlign:center, title row justifyContent:center) and the large 0/1,000 counter moved to centre below the subtitle (was right-aligned); order Title→Subtitle→Counter→Unlock. (2) HomeScreen pills: width 100%→80% + alignSelf:center (both cards centred). (3) GameScreen hint: removed the 5s setTimeout + 500ms fade — overlay now persists until the CORRECT tile is tapped, then removed INSTANTLY. Mechanism: hint stores its target value; an effect clears it the moment expectedNext advances past that value (a wrong tap leaves expectedNext unchanged, so the overlay stays). Still no GameScene.ts change. (4) SettingsScreen Purchases: both rows were dead — "Remove Ads Forever" row now onClick→navigate('/iap') (whole row tappable; inner OK button demoted to a span to avoid double-fire); "Restore Purchases" row onClick→toast t('campaign.placeholder_toast') "Available in full release" (added a local toast). DEVICE RE-VERIFY on SM-E146B: Corr1 counter textAlign:center cyan; Corr2 pill width 303.5px = 80% of the 379px padded container + alignSelf center; Corr3 overlay PRESENT after hint, PRESENT after a wrong tap, GONE immediately after the correct tap; Corr4 Restore→"Available in full release" toast, Remove Ads row→/iap. 130 tests + tsc clean. NOT pushed — awaiting approval. |
| 06 Jun 2026 | T-018b: Dual-trigger interstitial — 3min OR 5 completions (Sprint 4) | Brief premise mismatch (noted + adapted): brief assumed a `maybeShowInterstitial()` orchestrator, but the real T-018 service exposed `loadInterstitial`/`isCapElapsed`/`showInterstitial` called directly from ResultScreen, with the Remove-Ads guard living in ResultScreen. Honoured the brief's API by ADDING `maybeShowInterstitial()` as the single decision+show entry point (folds in the IAP guard via `useSettingsStore.getState().removeAdsPurchased` — established service pattern, same as campaignScores/dailyScores/soundService — and the dual-trigger read), and REMOVED the now-redundant exported `isCapElapsed()`. Added `incrementLevelCount()` (reads `levels_since_last_ad`, parses int default 0, writes +1). New `COUNT_KEY='levels_since_last_ad'`, `COMPLETION_CAP=5`. Decision: `timeElapsed >= 180_000 || levelCount >= 5`; conservative (no show) on Preferences read error. `showInterstitial()` now resets BOTH counters (timestamp→Date.now(), count→'0') only AFTER a confirmed `AdMob.showInterstitial()`; on throw the catch swallows and neither resets (retry-next-mount). ResultScreen effect now: native guard → `await incrementLevelCount()` → `await maybeShowInterstitial()` (count this completion before evaluating, so the 5th completion can fire); removed the unused `removeAdsPurchased` destructure (guard moved into service). admob.ts untouched (still TEST IDs + isTesting:true). New test file `interstitialAdService.test.ts`: 11 tests (8 brief-mandated cases 1-8 + 3 incrementLevelCount cases incl. push-to-5-fires). 130 existing tests unchanged → 141/141 green, tsc (tsconfig.app) clean. Files: interstitialAdService.ts, ResultScreen.tsx, interstitialAdService.test.ts. No locked files touched. Device check pending. |
| 06 Jun 2026 | B-004: ResultScreen campaign boundary gate check (Sprint 4) | Brief premise corrected: brief assumed `campaignGateService.isGateOpen('campaign2')`; the real service exposes `isCampaignUnlocked(campaignId:number)` (community gate) + `isPurchased(campaignId:number)` (reads the campaignN_purchased Preferences flag internally). Used those two — no duplicated gate/Preferences logic (Section 3). Added exported `isCampaignLocked(nextLevelId)`: maps 101→C2, 201→C3, else false synchronously (no async); locked = `!isPurchased && !isCampaignUnlocked` (Promise.all). Added `nextCampaignLocked` + `gateCheckPending` state; a new useEffect runs only when next id is 101/201 (computes via LevelManager.getNextLevelId; guarded by mode==='campaign' so daily's synthetic id 0 never hits getNextLevelId). NEXT LEVEL render wrapped: pending → dimmed `···` placeholder (no flash); locked → `CampaignLockedMessage`; else the unchanged button. PLAY AGAIN unchanged. Added exported `CampaignLockedMessage` (module fn in ResultScreen.tsx, not a new file): 🔒 "Campaign N Locked" + literal gate-status line + gold "GO TO CAMPAIGN SCREEN" → navigate('/campaign'); styled with var(--navy-card)/var(--navy-border)/var(--gold)/var(--white) to match CampaignScreen gate cards. Literals (no i18n keys) since locale files are outside this brief's single permitted file. Tests: new `ResultScreen.test.tsx`, 12 tests (6 isCampaignLocked logic cases 1-6, 3 render cases 7-9 via full ResultScreen render with mocked stores/services/children, 1 non-boundary render, 2 isolated CampaignLockedMessage). 141 existing unchanged → 153/153 green, tsc (tsconfig.app) clean. Device (SM-E146B, CDP): completing a 7×7 campaign boundary level on-device is impractical (random/shuffle layouts — only seeded Daily is automatable; Phaser canvas tiles aren't DOM-readable; Math.random-pinning blanks the Phaser renderer; short-timer + inter-turn-latency trap). Verified instead the exact gate-service inputs isCampaignLocked consumes, in the real bundle: with no purchase flags + gate unmet, CampaignScreen shows PRO/EXPERT 🔒 "0 / 1,000" (→ isCampaignLocked 101/201 = true); with campaign2_purchased=true, PRO flips to EARLY ACCESS + L101 unlocked (→ isCampaignLocked 101 = false). ResultScreen render swap covered by unit tests 7-9. Device restored to baseline (removeAdsPurchased=true, no purchase flags). Only ResultScreen.tsx modified; campaignGateService.ts untouched; admob.ts still TEST. |
| 07 Jun 2026 | B-005: Country flag before alias in YOU/LEADER panel (Sprint 4) | Brief premises corrected (verified first): (1) the YOU/LEADER alias is rendered ENTIRELY by the LOCKED LeaderPanel.tsx — ResultScreen/GameScreen only mount `<LeaderPanel levelId compact/>` and pass no alias/country, so the brief's "prepend flag in the caller" was impossible; (2) a `getFlag()` util was specified but `countryFlag()` already exists (reused, no new file/dup); (3) the leader's country wasn't fetched — fetchLevelLeader selected only alias/score/time_secs and LevelLeaderInfo had no country. Asked owner (AskUserQuestion) since the fix requires LOCKED files; owner approved unlocking LeaderPanel.tsx + campaignScores.ts. Changes: campaignScores.ts — added `country` to LevelLeaderInfo + fetchLevelLeader SELECT ('' when null). LeaderPanel.tsx — added exported pure `aliasFlag(country)` = countryFlag(code) for a real 2-letter non-XX code, else '' (suppresses the 🌐 globe placeholder per brief's no-placeholder rule); read `country` from settingsStore; prepend `aliasFlag(country)` before the YOU alias and `aliasFlag(leader.country)` before the LEADER alias (existing ellipsis/nowrap handles truncation). ResultScreen.tsx/GameScreen.tsx unchanged (they render the now-fixed LeaderPanel). countryFlag.ts unchanged. Tests: new LeaderPanel.test.tsx, 11 tests (7 aliasFlag policy: IN/DE/lowercase→flag, null/''/XX/wrong-len→''; 4 render: YOU flag+alias, YOU alias-only on XX, LEADER flag+alias, LEADER alias-only on no-country). 153 existing unchanged → 164/164 green, tsc clean. Device (SM-E146B, CDP DOM read — panel is React DOM, no 7×7 completion needed): set country=IN/alias=Zephyrv, started a campaign level → in-game LeaderPanel YOU row shows "🇮🇳 Zephyrv" (flag verified live); LEADER rows (levels 1-6) all default alias "Player" with no stored country → alias only, no flag, no crash (graceful fallback verified live). Positive LEADER-flag-with-country path covered by unit test #10 + identical aliasFlag helper (proven live via YOU) + country now fetched; not shown live only because the dev Supabase has no country-bearing leader rows. Device restored to baseline (country=CN, removeAdsPurchased=true). admob.ts still TEST. |
| 07 Jun 2026 | B-006: Country list expanded 23 → 155 (Sprint 4) | CountrySelector.tsx data expansion. Premise notes: a search input ALREADY existed (q state + name/code filter) — reused, not re-added; the data is `{code,name}` with the flag DERIVED via countryFlag(code) (no stored flag field — the established pattern), so kept that "or equivalent" shape. The brief's explicit list is 134 countries but test #1/DoD require ≥140 — included all 134 listed, KEPT the existing China (CN) entry (brief omits it, but removing it would strip flag/name for existing CN-set players incl. the test device baseline), and added ~20 more real Play-distribution countries (Bahamas, Barbados, Bhutan, Brunei, Burundi, Cape Verde, Congo (DRC), Côte d'Ivoire, Eswatini, Gabon, Gambia, Guyana, Lesotho, Liechtenstein, Maldives, Monaco, Montenegro, Sierra Leone, Suriname, Togo) → 155 total, satisfying ≥140 and the "all distribution countries" intent. Avoided XK (Kosovo — no standard flag emoji). To enable the required unit tests, exported `COUNTRIES` + a `Country` interface and extracted the existing inline filter into a pure exported `filterCountries(query)` (behaviour-identical; component now calls it) — single source of truth, no logic change. List sorted via existing `.sort(localeCompare)`. Tests: new CountrySelector.test.ts, 11 tests (8 brief cases: ≥140 count, India/Germany/Saudi present+flag, all entries valid 2-letter code/name/derived-flag, alphabetical sort, filter 'India'→[India], filter 'zzz'→[]; + 3 extras: no dup codes, case-insensitive/code match, empty query→full list). 164 existing unchanged → 175/175 green, tsc clean. Device (SM-E146B, CDP DOM): Settings → country selector shows 155 scrollable rows (Afghanistan→Zimbabwe, alphabetical); search 'Saudi'→🇸🇦 Saudi Arabia, 'Korea'→🇰🇷 South Korea; selecting South Korea updated the Settings row to "🇰🇷 South Korea". Device country restored to CN. Only CountrySelector.tsx changed; admob.ts still TEST. |
| 07 Jun 2026 | B-007 + B-008 + B-009: pill borders dotted, ResultScreen banner ad, win SFX fix (Sprint 4, bundled) | **B-007** HomeScreen.tsx AchievementCard pill border `2px solid`→`2px dotted` (one change covers both pills via shared component; EXPERT cyan #00f5ff / PRO purple #9B59B6; inner checkbox circle left solid). **B-008** new src/services/bannerAdService.ts (admob.ts stays LOCKED — brief sanctioned a separate service file): showResultBanner()/hideResultBanner() via AdMob.showBanner/removeBanner, BannerAdSize.ADAPTIVE_BANNER + BannerAdPosition.BOTTOM_CENTER, TEST unit ca-app-pub-3940256099942544/6300978111 (isTesting:true), idempotent show + shown-guard on hide. ResultScreen.tsx: new native-only useEffect shows banner 500ms after mount, hides on unmount cleanup, early-returns when removeAdsPurchased; removed the "Ad · SPRINT 4" placeholder span (kept the div as layout spacing). NOTE for T-015: a LIVE banner ad unit must be created in the AdMob console (vorakyretail1@gmail.com / pub-7932168293321470) — none exists yet; swap the test ID + create the unit before launch. Observation: BOTTOM_CENTER banner overlays the BottomNav footer on ResultScreen (per brief spec) — candidate for T-015 polish (e.g. margin or repositioning). **B-009** sfx_win.mp3 replaced. Premise mismatch: brief's Option A (download CC0) and Option B (ffmpeg) both unavailable — ffmpeg/ffprobe are NOT installed in this env and web binary downloads aren't possible; used Option C, consistent with the original 5 synthesized SFX: a node+@breezystack/lamejs (ESM) generator synthesized an ascending C-major arpeggio (C5-E5-G5-C6) resolving into a sustained chord with a 1.2s master fade-out — 3.34s, 53.5KB, peak-normalized 0.9, natural ending (no abrupt cut). Old asset was 35.9KB/short. Tests: new src/services/bannerAdService.test.ts (4 tests: test unit/size/position, show-once idempotency, hide+re-show, hide no-op when not shown; uses resetModules+clearAllMocks per test). 175 existing unchanged → 179/179 green, tsc clean. Device (SM-E146B, CDP+screencap): B-007 both pills border-style=dotted (cyan+purple); B-008 ResultScreen shows green "Test Ad · 320x50" banner at bottom, "Ad · SPRINT 4" text gone, banner removed after navigating away (cleanup), and NOT shown when removeAdsPurchased=true (guard); B-009 new sfx_win.mp3 loads on device with duration 3.34s and plays. Device restored (country=CN, removeAds=true, test keys removed). admob.ts untouched (still TEST). |
| 07 Jun 2026 | B-010: Countdown modifier partial reveal — 50% tiles stay visible (Sprint 5) | GameScene.ts unlocked for this brief. Problem: after the 3s countdown hide ALL tiles went blank → unmemorisable on 5×5/6×6/7×7. Fix: after hide, Math.ceil(total/2) tiles stay permanently visible (first target guaranteed). Added private `countdownPermanentlyVisible: Set<string>` (reset in the runId block alongside countdownFirstSeen/countdownHidden); exported pure `selectCountdownVisibleTiles(grid, gridSize, direction)` — collects untapped cells, guarantees the first-target cell (ascending→value 1, descending→value n²), Fisher-Yates shuffles the rest, fills to visibleCount; called once at the instant the first tile hides (guarded by countdownHidden.size===0). isVisible() countdown branch now returns true for permanently-visible keys before the hidden check. Tap handling/hit-rects untouched (visible tiles stay tappable; hidden ones are blank but still hit-testable as before). Tests: new GameScene.countdown.test.ts, 8 tests (4×4=8, 5×5=13, 6×6=18, 7×7=25 counts; value-1 / value-49 always present across 25 runs; tapped tiles excluded; no dups). Importing GameScene.ts in jsdom throws (Phaser module-init draws to canvas), so the test stubs 'phaser' with a Scene base class via vi.mock — keeps the pure function importable without real Phaser; production change stays GameScene.ts-only. 179 existing unchanged → 187/187 green, tsc clean. Device (SM-E146B, CDP+screencap; injected completedLevels{1..97} to unlock, removeAds=true): 8.1 L67 5×5 — all 25 visible at start, after hide exactly 13 visible incl '1', 12 blank; 8.2 restart — '1' always visible, visible set differs run-to-run (random); 8.3 L70 4×4 — 8 visible incl '1', 8 blank; 8.4 L98 7×7 — 25 visible incl '1', 24 blank. Full completion not scripted (countdown grids are random-layout + the 30-90s timers vs inter-turn latency 'timer trap' — same limit noted in B-004/B-005); tappability is structurally unchanged by this view-only change. Device restored (country=CN, removeAds=true, completedLevels→baseline). Only GameScene.ts changed; all other locked files untouched. |
| 07 Jun 2026 | B-011: C2+C3 level redesign — progressive modifiers 101–300 (Sprint 5) | levels.json unlocked for this brief (returns to LOCKED after). Replaced all 200 levels 101–300 with the designed progression; C1 (1–100) kept byte-identical (verified via diff of id 1–100 lines: IDENTICAL), no entries beyond 300. Generated via a throwaway node script (deleted) that expands [grid,modifier,count] segments into consecutive ids, derives timeLimit + stars from grid (3→15s[7,10,13] / 4→30s[15,21,27] / 5→45s[22,31,40] / 6→60s[30,42,54] / 7→75s[37,52,67]), and serializes one-level-per-line matching the existing format. C2 (101–200): pack 4, direction "descending", progression none→shuffle→mirror→fog→countdown across 3×3→7×7 with reprise + 7×7 fog(4)/countdown boss stretch. C3 (201–300): pack 5, direction "ascending" (runtime-overridden — expert uses a random permutation, gameStore.ts:111), same modifier arc with harder large-grid combos + single 7×7 countdown finale (300). direction fields for 101–300 are cosmetic (overridden at runtime by difficulty pro/expert per the confirmed facts); modifier IS live (passed to GridEngine). Validation (Section 5.4): C2=100, C3=100, C1=100, ID gaps none, invalid entries 0, wrong time limits 0, total 300; modifier distribution {none:48, shuffle:45, mirror:39, fog:34, countdown:34} (=200, matches spec segments). No new unit tests (data-only); 187/187 existing green, tsc clean. No device check (per brief — JSON read identically by the engine; C2/C3 gameplay QA deferred to the level-skip tool). Only levels.json changed. admob.ts still TEST. B-010 countdown partial-reveal auto-applies to the new C2/C3 countdown levels (lives in GameScene, keys on modifier).|
| 07 Jun 2026 | B-012: House ad slot — internal Gazetica promo, remove AdMob banner (Sprint 5) | Reverted B-008: DELETED src/services/bannerAdService.ts + bannerAdService.test.ts; removed the banner mount/unmount useEffect + its import from ResultScreen.tsx; the AdMob test banner unit (…/6300978111) is no longer referenced anywhere (grep-confirmed). New src/components/GazeticaPromoCard.tsx — static cross-promo of 4 upcoming Gazetica games (Flow Lines/Word Drop/Pulse Grid/Echo Trail), no network/SDK/AdMob. Three variants by result state: compact (44px, campaign win — single-line "ALSO BY GAZETICA: <name> →", no card), medium (80px, other win — navy card + colour-circle icon + name + tagline + Coming Soon badge), full (120px, fail/timeout — "MORE FROM GAZETICA" header + name + 2-line description + "DISCOVER →" + Coming Soon). ResultScreen picks variant: !isComplete→full : campaign→compact : medium. Tap → Browser.open('https://gazetica.com/games.html') via @capacitor/browser (same native-guarded pattern as AboutScreen). Rotation: module-level promoIndex advanced once per MOUNT via a useState initializer (DEVIATION from the brief's render-body promoIndex++, which would over-increment because ResultScreen re-renders several times per visit; the initializer is read-only state — no extra re-renders — and gives exactly one game per mount; shipped build is a Vite production React build so StrictMode does not double-invoke). Tests: deleted 4 banner tests, added 8 GazeticaPromoCard tests (variant rendering, rotation/cycle, Coming Soon badge, compact-has-no-card, Browser.open on tap). 183 prior-minus-banner + 8 = 191/191 green, tsc clean. Device (SM-E146B, CDP+screencap): medium (daily win) — Flow Lines card, no AdMob banner, no footer overlap; rotation — Flow Lines→Word Drop→Pulse Grid→Echo Trail→Flow Lines across 5 ResultScreen mounts (cycles, 1/mount); full (daily timeout) — Word Drop full card with header/description/DISCOVER/Coming Soon; tap → Chrome Custom Tab opened to gazetica.com games page; campaign fail also correctly showed full variant. Compact (campaign WIN) not device-captured — random-grid campaign completion isn't scriptable (geometry/timer trap, per B-004/B-005/B-010); covered by unit test #1 + the proven variant ternary. NO AdMob banner anywhere (confirmed in all screenshots; service deleted). Device restored (country=CN, removeAds=true). Files: ResultScreen.tsx, GazeticaPromoCard.tsx(+test); bannerAdService.ts/.test deleted. admob.ts still TEST (untouched). |
| 07 Jun 2026 | DEV-001: Dev level-skip tool + test-state reset (Sprint 5) | SettingsScreen.tsx: added a bottom DEV TOOLS section (amber "⚠ DEV TOOLS" label, top-border separator). "Jump to Level" numeric input + GO: validates 1–300 (else inline error "Enter a level between 1 and 300"), writes Preferences currentLevel, then replicates CampaignScreen's exact startLevel flow — C1 (1–100) no difficulty, C2 (101–200) 'pro', C3 (201–300) 'expert' — and navigate('/game'); logs [DEV] Jumping to level {n}. "Reset Test State" button removes removeAdsPurchased/completedLevels/currentLevel/campaign2_purchased/campaign3_purchased/player_tier and sets country=IN, shows "Test state reset ✓" 2s, logs [DEV] Test state reset. PREMISE CORRECTION: brief §2.1 specified `import.meta.env.DEV` (false in ALL builds → §8 device check would be impossible); used the project's established gate `import.meta.env.DEV || import.meta.env.VITE_DEV_TOOLS === 'true'` (same as AboutScreen.tsx/consentService.ts) so the section appears in `npm run dev` AND the on-device dev build (VITE_DEV_TOOLS=true npm run build) but is absent from a plain release build. Tests: new SettingsScreen.test.tsx, 6 tests (renders when DEV true; renders when VITE_DEV_TOOLS=true & DEV false; ABSENT when both false; invalid 0/301/abc → error + no nav; valid 150 → startLevel(150,'campaign','pro')+nav+currentLevel pref; 250→expert & 50→undefined). 191 prior → 197/197 green, tsc clean. §6 production check: plain `npm run build` → grep dist for DEV TOOLS/Jump to Level/[DEV] = ABSENT (Rolldown DCE'd the statically-false gate). Device (SM-E146B, dev-tools APK, CDP+screencap+logcat): DEV TOOLS visible at Settings bottom; jump 150 → game at Level 150 (C2, 5×5 fog, pro); jump 250 → Level 250 with cyan EXPERT tier label+border + random NEXT (expert); jump 0 → error shown, stayed on /settings, no [DEV] Jumping log; Reset → confirmation + [DEV] Test state reset; logcat captured "[DEV] Jumping to level 150" and "…250". Only SettingsScreen.tsx changed (+ test + this log). NOTE (flagged to owner, NOT committed): unrelated out-of-band working-tree changes present — android/app/build.gradle has signingConfigs.release with PLAINTEXT passwords, AndroidManifest.xml added BILLING permission, and an untracked-but-NOT-gitignored gazetica-numtap.jks keystore; excluded from this commit (locked android/ + secrets). |
| 08 Jun 2026 | T-019: Real IAP billing via Google Play Billing 6.2.1 (Sprint 5) | Replaced all placeholder IAP with a real BillingClient bridge. NEW android/app/src/main/java/com/gazetica/numtap/BillingPlugin.java — Capacitor @CapacitorPlugin(name="Billing") wrapping Play Billing 6.2.1: initialise (startConnection), queryProducts (queryProductDetailsAsync, all 4 INAPP SKUs, caches ProductDetails), purchase (launchBillingFlow; queries details first if uncached; result delivered via onPurchasesUpdated → resolves the kept-alive call; acknowledges non-consumables, leaves hint_pack for JS consume; USER_CANCELED → success:false), restorePurchases (queryPurchasesAsync INAPP; acks unacked non-consumables), consume (consumeAsync for hint pack). MainActivity.java registers it via registerPlugin(BillingPlugin.class) in onCreate (Capacitor 8 API — brief's add() is the old Cap-3 form; corrected). NEW src/services/billing.ts — TS wrapper (registerPlugin('Billing')): initialiseBilling/queryProducts/purchaseProduct/restorePurchases/consumePurchase; native-only + defensive (web → safe no-op/{success:false}). preferences.ts: added CAMPAIGN2_PURCHASED/CAMPAIGN3_PURCHASED keys (read by locked campaignGateService.isPurchased). Wiring — IAPScreen.tsx: Remove Ads BUY → purchaseProduct(numtap_remove_ads)→setRemoveAds(); Hint Pack BUY → purchaseProduct(numtap_hint_pack_5)→addHints(5)+consumePurchase(token); Pro/Expert UNLOCK → purchaseProduct(numtap_campaign2/3)→prefSetBool(CAMPAIGNn_PURCHASED,true); Restore → restorePurchases()→re-apply entitlements; live prices via queryProducts() on mount (override placeholders); analytics.iapPurchase on success; USER_CANCELED silent. SettingsScreen.tsx: Restore Purchases row → restorePurchases() re-apply. App.tsx: initialiseBilling() on native startup (after AdMob). Purchase-option IDs (remove_ads_buy etc.) are Play-Console-side metadata; the BillingClient purchases by product ID, so no app code references them. NO resolutionStrategy needed — billing:6.2.1 is the sole version in releaseRuntimeClasspath (verified). Tests: new billing.test.ts, 9 tests (native passthrough, invalid-SKU/throw → success:false, restore array, queryProducts, consume forwarding, web degradation). 197 prior + 9 = 206/206 green, tsc clean. Builds: npm run build ✓, bundleRelease ✓ (BillingPlugin.java compiles vs 6.2.1; signed AAB 9.0MB). Device (SM-E146B, debug APK + CDP + logcat): 'Registering plugin instance: Billing' ✓; initialise called on startup, Finsky responded ✓; tap BUY → native purchase call → com.android.billingclient.api.ProxyBillingActivity (Play billing sheet) launched, no crash ✓; cancel (BACK) → returns to MainActivity, app alive ✓; Restore → native restorePurchases call, no crash ✓; no FATAL/AndroidRuntime crash. NOTE: full purchase COMPLETION (entitlement granted) needs the Play-distributed internal-testing-track AAB + license testers (sideloaded debug isn't license-matched) — owner uploads to verify the paid path; local check proves wiring/registration/flow-launch/no-crash. AdMob untouched (TEST IDs; T-015 last). Device restored (country=CN, removeAds=true, campaign flags cleared). |
| 08 Jun 2026 | F-002: Pause modal on back button during gameplay (Sprint 5) | NEW src/components/PauseModal.tsx — presentational overlay (PAUSED cyan title + RESUME/RESTART/QUIT full-width buttons; scrim tap = resume) + pure `quitTarget(mode)` helper (campaign→/campaign, else→/home); Phaser-free so it's unit-testable (GameScreen imports Phaser, which throws under jsdom). GameScreen.tsx: registers a @capacitor/app `backButton` listener on mount (native-only, removed on unmount) — while paused→resume; while status==='playing'→open pause; else fallback to /home (never trap). openPause: pauseGame() (status→paused→TimerComponent freezes) + phaserRef.scene.pause('GameScene'); resumePause: scene.resume + resumeGame(); restartFromPause: scene.resume + startFreePlay/startDailyChallenge/startLevel (campaign passes difficulty for C2/C3 'pro'/'expert', omits for C1 'easy' so level.direction e.g. descending is preserved); quitFromPause: navigate(quitTarget(mode)). pauseOpenRef mirrors state so the once-registered listener avoids stale closures. Background music keeps playing (audio not touched). GameScene.ts NOT modified (permanently locked — pause via scene manager from GameScreen). Tests: new PauseModal.test.tsx, 6 tests (renders PAUSED + 3 buttons; RESUME/RESTART/QUIT invoke callbacks; quitTarget mapping incl. endless→/home; scrim tap → resume). 206 prior + 6 = 212/212 green, tsc clean. Device (SM-E146B, dev-tools debug APK + jump tool + CDP): back on campaign L81 → PAUSED modal (no nav away); timer frozen at 69 across 4s (paused ✓); RESUME → modal gone, timer resumed (69→66 counting); back→RESTART → L81 reset (timer→full 88, same level); back→QUIT → /campaign; tapping a grid coord while modal open → NEXT stayed 01 (no tap registered) + scrim resumed. AdMob untouched (TEST). Device restored (country=CN, removeAds=true). |

| 08 Jun 2026 | T-015: AdMob TEST→LIVE IDs (Sprint 5 — final code task) | Swapped all AdMob test IDs to live production. App ID in android/app/src/main/AndroidManifest.xml (ca-app-pub-3940256099942544~3347511713 → ca-app-pub-7932168293321470~5301760306). Ad-unit IDs in src/services/admob.ts AD_UNITS: REWARDED → ca-app-pub-7932168293321470/8916890465, INTERSTITIAL → ca-app-pub-7932168293321470/5496520200 (these constants are the single source — interstitialAdService.ts + rewardedAdService.ts import AD_UNITS, so the swap propagates to the real ad paths). DEVIATION (owner-approved): the brief said "2 files only" and never mentioned isTesting, but the live ad requests hardcoded isTesting:true in 4 spots (admob.ts showRewarded+showInterstitial, interstitialAdService.ts, rewardedAdService.ts) — leaving it on means production serves Google TEST ads (no revenue) and contradicts the brief's own §4.4 ("will show real ads") + HANDOVER's T-015 definition. Owner chose to remove isTesting everywhere → 4 source files changed (admob.ts, AndroidManifest.xml, interstitialAdService.ts, rewardedAdService.ts). HANDOVER's "remove UMP EEA debug gate" needed NO action: that gate is in consentService.ts (not admob.ts), already behind DEV_TOOLS and DCE'd out of plain `npm run build` — production already safe; left untouched. Verify: grep test IDs (src + android/app/src/main, incl. rebuilt bundle after cap sync) = ZERO; live IDs = 3 source lines (admob ×2 + manifest) + the compiled bundle; isTesting grep over src = ZERO. tsc clean (tsconfig.app.json). 212/212 tests green (15 files). npm run build OK; npx cap sync android OK; gradlew bundleRelease BUILD SUCCESSFUL (signed via keystore.properties). AAB: android/app/build/outputs/bundle/release/app-release.aab, 9,025,760 bytes, 2026-06-08 11:54. versionCode still 3 — bump to 4 before the Play upload (post-approval step). Release candidate for Play Store submission. |

| 08 Jun 2026 | B-013 + B-014 + F-003: Onboarding fix, Ad Preferences, How-To-Play access (Sprint 5) | **B-013 (HowToPlayScreen.tsx + 6 locales):** slide 1 step1 literal "…in order from 1 upward — the HUD shows what's next." → "…Tap them in sequence — the HUD shows your next target."; slide 2 step4 literal "…target lights up gold for 5 seconds." → "…target tile glows cyan until you tap it correctly."; slide 3 modes array dropped ENDLESS+SPEED (don't exist) and added FREE PLAY (♾ icon) → now CAMPAIGN/DAILY/FREE PLAY. i18n (all 6: en/de/fr/ko/pt/es): how_to_play.sub "Master Numtap in 30 seconds"→"Train your eyes. Race the clock."; mode_campaign_sub→"300 levels across 3 campaigns · 3×3 to 7×7 grids · 3-star rating"; mode_daily_sub→"3 daily challenges · Global leaderboard · One attempt per day"; removed mode_endless/_sub + mode_speed/_sub, added mode_freeplay/_sub (translated per locale). CLEANUP (spot-check driven): the unused how_to_play.step1–step4 + demo_label + demo_hint i18n keys (component renders hardcoded literals, not these — they were deferred-<Trans> scaffolding) still carried the OLD contradictory copy ("from 1 upward", "reveal the target for 5 seconds"); deleted from all 6 locales so no stale onboarding copy ships; updated the component's header comment. **B-014 (consentService.ts):** the AboutScreen "Ad Preferences" row was ALREADY correctly wired (action: reopenForm → AdMob.showPrivacyOptionsForm) — the "not responding" premise is environmental (showPrivacyOptionsForm has nothing to present when no privacy-options form is available, e.g. non-EEA region, or when consent state is stale). Fix = made reopenForm robust: it now calls AdMob.requestConsentInfo(buildRequestOptions()) FIRST (refreshes privacy-options availability + re-applies the EEA debug gate so the form is reachable from the non-EEA SM-E146B in dev builds) before showPrivacyOptionsForm. **F-003 (AboutScreen.tsx):** added a "How to Play" row as the FIRST LINKS entry (before legal rows) that navigates to /how-to-play (added a `route` field to the link type + useNavigate in the component; about.how_to_play key added to all 6 locales). NOTE: HowToPlayScreen's Skip/Play both setOnboardingShown + navigate('/home') — harmless when entered from About (returns home). Tests: new HowToPlayScreen.test.tsx (3: slide-1 no "from 1 upward"/"30 seconds"; slide-3 no ENDLESS/SPEED; slide-3 has "300 levels"+"FREE PLAY") + AboutScreen.test.tsx (2: How-to-Play row → navigate('/how-to-play'); Ad Preferences row → reopenForm) — both use the real i18n singleton (en) so translated rows assert real copy. 212 → 217/217 green (17 files), tsc clean (tsconfig.app.json). npm run build OK. CONTENT SPOT-CHECK on dist (standing rule): new strings present incl. translations (fr "JEU LIBRE", ko "자유 플레이"); stale strings ABSENT ("from 1 upward", "Master Numtap in 30", "lights up gold for 5 seconds", "How many rounds in 3 minutes", "reveal the target for 5 seconds"). Files: HowToPlayScreen.tsx, AboutScreen.tsx, consentService.ts, 6× locales, + 2 new test files. admob.ts untouched (live IDs intact). **DEVICE VERIFICATION (SM-E146B, VITE_DEV_TOOLS debug APK, CDP+screencap+logcat):** About now lists "How to Play" as the FIRST row before legal rows (F-003 ✓); tapping it → /how-to-play. Slide 1: "Tap them in sequence", subtitle "Train your eyes. Race the clock.", NO "from 1 upward"/"30 seconds" ✓. Slide 2: "…glows cyan until you tap it correctly", NO gold-5s ✓. Slide 3: rows = [CAMPAIGN, DAILY, FREE PLAY], has "300 levels", NO ENDLESS/SPEED ✓. **B-014 BLOCKER (OWNER, AdMob console — NOT code):** tapping Ad Preferences runs the fixed reopenForm — logcat confirms requestConsentInfo fires with debugGeography:1 (EEA) + the test-device hash — but UMP returns "Publisher misconfiguration: Failed to read publisher's account configuration; … lack of configured form(s). Received app ID: ca-app-pub-7932168293321470~5301760306." i.e. the LIVE app (post-T-015) has NO GDPR/privacy message configured in AdMob Console → Privacy & messaging. reopenForm catches it gracefully (warn, no crash, stays on About). The form will not display until the owner creates+publishes a consent message for the live app ID. The original "not responding" premise = exactly this. OBSERVATION (out of scope, flagged): the HOME screen still shows ENDLESS/SPEED tiles (separate `home` section, not how_to_play) — brief said those modes "don't exist". **OWNER OVERRIDE (post-device-check): the brief was WRONG — ENDLESS + SPEED ARE live.** Restored both to slide 3 with their ORIGINAL descriptions (en: ENDLESS "How many rounds in 3 minutes?", SPEED "4×4 · Halved timer · 2× score"; translated in all 6 locales) → slide 3 now lists 5 modes: CAMPAIGN, DAILY, FREE PLAY, ENDLESS, SPEED. FREE PLAY icon changed ♾️→🎮 (gamepad) so ENDLESS can reclaim its original ♾️. Test #2 inverted: was "slide 3 no longer lists ENDLESS/SPEED" → now "slide 3 lists all five live modes". Re-verified: 217/217 tests, tsc clean, build OK, dist spot-check shows all 5 modes incl. translations (fr VITESSE, ko 엔드리스). Awaiting push approval. |

| 08 Jun 2026 | B-015: Fix YOU Time showing dash in LeaderPanel (ResultScreen) | PREMISE CORRECTION: the brief assumed ResultScreen passes a completion-time prop that wasn't wired. Reality: LeaderPanel takes only levelId/compact and derives the YOU row from getPlayerPB(levelId) — and getPlayerPB (in LOCKED campaignScores.ts) hardcodes `timeSecs: null` ("until T-002 adds it"), so YOU Time was ALWAYS a dash and never wired. gameStore has timeElapsed (no timeRemaining); tickTimer sets it, so timeElapsed IS the completion time (the brief's levelTimeLimit−timeRemaining). FIX (2 files, campaignScores untouched): LeaderPanel.tsx — added optional `youTimeSecs?: number|null` prop; YOU Time row now renders `youTimeSecs ?? playerPB.timeSecs` as `Xs` (falls back to dash when absent, e.g. the in-game compact panel, unchanged). ResultScreen.tsx — passes `youTimeSecs={isComplete ? Math.round(timeElapsed) : currentLevel.timeLimit}` (win → actual seconds; fail/timeout → time limit, matching LEADER + the brief). The brief permitted the LeaderPanel prop change ("unless a prop interface change is also needed"). Tests: LeaderPanel.test.tsx +2 (#12 no prop → dash; #13 youTimeSecs=6 → "6s"); ResultScreen.test mocks LeaderPanel so unaffected. 211 → 213/213 green, tsc clean. DEVICE VERIFICATION (mandatory; SM-E146B debug APK — note DEV-002 removed the jump tool, so reached L1/L2 via normal campaign play): completed Campaign L1 and L2 (3×3). Both result screens show YOU Time = **6s** (header + LEADER format match), no dash; L1 also captured the FAIL path (timeout) showing YOU Time = 30s = timeLimit (fix's fail branch ✓). TECHNIQUE: campaign grids are random + the timer keeps running across tool-call latency (HANDOVER timer-trap), and screen-off only pauses Phaser's render loop, NOT the JS timer — so completion required freezing the JS event loop via CDP Debugger.pause() (held in a background WS) while reading the grid, then Debugger.resume()+tap 1–9. NOTE (test data): completing L1/L2 in campaign mode fired submitCampaignScore → added a couple of alias=Zephyrv rows to Supabase campaign_scores (folds into the owner's known pre-launch test-row cleanup). Awaiting push approval. |

| 08 Jun 2026 | B-016 + B-017: In-play LeaderPanel time + Ad Preferences UMP guard | **B-016 (NO CODE CHANGE — Option B confirmed, by design):** the compact in-play LeaderPanel (GameScreen, compact={true}) YOU Time is a dash because it reads getPlayerPB(levelId) from LOCKED campaignScores.ts, which returns `timeSecs: null` UNCONDITIONALLY (no Supabase fetch, no local time storage — "until T-002"). CORRECTION to the brief's Option B wording: the dash is PERMANENT, not "resolves on next visit" — getPlayerPB has no time source at all (Option A inapplicable: it does not fetch Supabase). Score DOES show (from bestScores), only Time is null. A real fix needs T-002 (store/fetch PB time) which requires unlocking campaignScores.ts — owner-scoped; out of this brief's allowed files (LeaderPanel/GameScreen only). Per the brief, accepted no-change. **B-017 (consentService.ts only):** the brief's premise (handler calls showIfRequired() no-op) was outdated — B-014 already wired About→Ad Preferences (action: reopenForm) to requestConsentInfo()+showPrivacyOptionsForm(). B-017 deliverable = added the availability guard: reopenForm now reads `const info = await requestConsentInfo(...)` and only calls `showPrivacyOptionsForm()` when `info.isConsentFormAvailable` (non-EEA → silent no-op, no error/alert). API-name note: brief said AdMob.getConsentInfo()/isConsentFormAvailable — the real API is requestConsentInfo() returning AdmobConsentInfo.isConsentFormAvailable. AboutScreen unchanged (already calls reopenForm). 213/213 tests green (no new tests — native-only path), tsc clean. DEVICE VERIFICATION (SM-E146B, VITE_DEV_TOOLS debug APK so the EEA debug gate makes a form available): **B-017 ✅ the UMP privacy-options dialog NOW APPEARS** ("numtap asks for your consent… Consent / Do not consent / Manage options"); logcat shows UMP `Action[load_complete]:{status:ok}` with NO "Publisher misconfiguration" — i.e. the owner has since configured+published a consent form in the AdMob console for the live app ID (the B-014 blocker is resolved). **B-016 ✅ confirmed dash:** in active L1 play the compact panel shows YOU Score 1,580 (PB) but Time — (dash); LEADER 1,780 / 11s renders fine — matches Option B by design. Awaiting push approval. |

| 08 Jun 2026 | T-002: Store + fetch PB time locally for in-play LeaderPanel YOU row | Adds local PB-time storage parallel to bestScores so the compact in-play LeaderPanel (and ResultScreen) YOU Time shows a real value instead of the permanent dash from B-016. **Files:** preferences.ts (not locked) — added PREF_KEYS.BEST_TIMES ('bestTimes'). settingsStore.ts (unlocked) — added BestTimes interface, `bestTimes: BestTimes` state (default {}), `setBestTime(key,time)` action (in-memory set + prefSetJSON, mirroring the bestScores write-through), and load via prefGetJSON in loadFromPreferences. campaignScores.ts (unlocked) — getPlayerPB now returns `timeSecs: bestTimes[campaign_${levelId}] ?? null` (was hardcoded null); submitCampaignScore now stores the PB time after the insert. PREMISE CORRECTION (brief 2b): the brief's gate `if (score > currentBest) setBestTime(...)` CANNOT fire — ResultScreen calls recordLevelComplete() (which sets bestScores = max(prev,score)) BEFORE submitCampaignScore() reads it, so on a real PB `score === bestScores[key]` and a strict `>` is always false. Used `score >= (bestScores[key] ?? 0)` (robust to that ordering: identifies the run that set/holds the best score). Also store regardless of the Supabase result (local panel must work offline, mirroring the local score write) and guard `timeSecs != null`. LeaderPanel.tsx / ResultScreen.tsx unchanged (B-015 already does `youTimeSecs ?? playerPB.timeSecs`). Tests: settingsStore.test.ts +2 (setBestTime stores/overwrites), new campaignScores.test.ts +4 (getPlayerPB null first-visit; stores on PB; unchanged on non-PB; null-guard). 213 → 219/219 green, tsc clean. DEVICE VERIFICATION (SM-E146B, pm clear → fresh install): (1) first play compact panel YOU Score —/Time — (no PB, correct); (2) complete L1 → ResultScreen YOU Time 6s (B-015 regression OK); (3) **PLAY AGAIN → in-play compact YOU Time = 6s** ✓; (4) force-stop + cold relaunch → L1 in-play YOU Time still 6s (Preferences persistence ✓). DEVIATION FROM BRIEF'S DEVICE STEPS (positive): the brief expected dash after the 1st completion and the time only after a 2nd — but the time correctly appears from the FIRST replay (one completion), because submitCampaignScore stores the PB time on the completing run (in-memory set is immediate). One completion suffices, not two. Note: in-play time shows the 1-decimal Supabase value (Math.round(timeElapsed*10)/10), here 6s; ResultScreen shows Math.round(timeElapsed)=6s (B-015) — consistent. Awaiting push approval. |

| 08 Jun 2026 | B-018 + B-019: i18n audit (HowToPlay + toasts) + Ad Preferences UMP unconditional | **B-018 (i18n audit):** ran the full grep sweep across src/components. Most UI already uses t() (GameScreen game.toast_no_ad/no_hints, DailyHub daily.*, etc. — false positives were comments). Real hardcoded user-facing strings found + fixed: HowToPlayScreen.tsx (step1, step2, step3, step4 literals, grid-demo caption, NEXT button), ResultScreen.tsx ("Difficulty" breakdown label), IAPScreen.tsx (Purchase complete/failed, Purchases restored/No purchases toasts), SettingsScreen.tsx (restore toasts — share the iap.* keys). Added 11 new keys × 6 locales (DE/FR/KO/PT/ES translated, not placeholders): how_to_play.step1-4/grid_hint/next, result.difficulty, iap.toast_purchase_complete/purchase_failed/restored/no_restore. Keys placed under existing sections (used the established `how_to_play.` prefix, not the brief's `howtoplay.` — consistency with the component's existing t('how_to_play.*') calls). Parity: all 6 locales = 243 keys, brief's parity script → all OK. AUDIT EXCLUSIONS (documented, deliberate): (a) LeaderPanel.tsx YOU/LEADER/Score/Time are hardcoded but the file is in THIS brief's LOCKED list (brief contradiction — it's also in the manual-check list) → cannot touch; flagged. (b) CountrySelector country names = proper nouns (standard to leave untranslated; 195×5 out of scope). (c) GazeticaPromoCard cross-promo copy for upcoming games (titles are brand names; "Coming Soon") → flagged for owner decision, not translated. **B-019 (consentService.ts):** removed the B-017 `if (info.isConsentFormAvailable)` guard around showPrivacyOptionsForm — after the user consents/declines, UMP reports isConsentFormAvailable=false, so the guard blocked re-opens (the user taps Ad Preferences precisely to CHANGE their choice). Now calls showPrivacyOptionsForm() unconditionally inside try/catch (non-EEA / no-form → throws → silent no-op). KEPT the preceding requestConsentInfo(buildRequestOptions()) (deviation from the brief's literal example, which dropped it) — it carries the EEA debug gate that makes the form reachable on the non-EEA test device and refreshes state; the brief's core instruction (remove the guard) is honoured. 219/219 tests green (string-wiring + guard removal, no new tests per brief), tsc clean. dist spot-check: KO/DE/FR new strings bundled. DEVICE VERIFICATION (SM-E146B, VITE_DEV_TOOLS debug APK): B-018 — switched language KO → About → How to Play: all step cards + caption + NEXT (다음 →) + Skip in Korean ✓; switched DE → German ✓ (step1 "Ein Raster aus gemischten Zahlen…", caption "Gold ✓ = zuletzt getippt…", NEXT "WEITER →"). B-019 — About → Ad Preferences → UMP dialog appears (load_complete ok); tapped "Do not consent"; back → Ad Preferences AGAIN → **UMP dialog appears a 2nd time** ✓ (regression fixed). Device language restored to EN. Awaiting push approval. |

| 09 Jun 2026 | F-004: Replace default Android robot icon with Numtap brand icon | Replaced the Capacitor default (robot) launcher icon with the brand mark: gold #FFD700 rounded square + navy #07111F "N". CURRENT STRUCTURE (before): mipmap-anydpi-v26/ic_launcher.xml + ic_launcher_round.xml were already standard adaptive icons (`@color/ic_launcher_background` + `@mipmap/ic_launcher_foreground`); mipmap-{m,h,xh,xxh,xxx}dpi each held ic_launcher.png + ic_launcher_round.png + ic_launcher_foreground.png (robot); values/ic_launcher_background.xml color = #FFFFFF. KEY POINT: on Android 8+ the launcher renders the ADAPTIVE icon, so replacing only ic_launcher.png (the brief's Pillow script) would NOT change the on-device icon — the robot lives in ic_launcher_foreground.png on a white background color. FIX: scripts/generate_icon.py (Pillow, kept in repo) regenerates per density, 4× supersampled + LANCZOS for crisp edges: (1) legacy ic_launcher.png + ic_launcher_round.png (48/72/96/144/192) = gold rounded square (25% radius ≈ 18dp) + centred navy N; (2) adaptive ic_launcher_foreground.png (108/162/216/324/432 = the full 108dp layer) = navy N only on transparent, sized 46% so it sits in the central safe zone. Set values/ic_launcher_background.xml color → #FFD700. Adaptive XML UNCHANGED (already the correct standard layout — no need for the brief's alt approach of pointing foreground at @mipmap/ic_launcher). Font: Consolas Bold (monospace, ~Space Mono) with Arial-Bold/DejaVu fallbacks (no Space Mono ttf in the project). cap sync + assembleDebug + clean reinstall (uninstall+install to bust the launcher icon cache). 219/219 tests, tsc clean (icon change is build-only, no code impact). DEVICE VERIFICATION (SM-E146B): App Info page shows the new icon next to "numtap" — gold disc with navy N (system-masked adaptive icon); robot gone. (Launcher/recents screencaps came back black due to transition/secure-surface; App Info is the definitive render.) Files: 15 PNGs (5 densities × ic_launcher/_round/_foreground) + values/ic_launcher_background.xml + scripts/generate_icon.py. Awaiting push approval. |

| 09 Jun 2026 | F-005: Gem economy overhaul + Rescue Flash mechanic (Sprint 5 retention) | Investigated first (per brief STOP CONDITIONS). STOP-CONDITION CHECK: #2 timeRemaining = timeLimit − gameStore.timeElapsed (React-derivable ✓), #3 tilesRemaining = count !tapped in gameStore.grid (✓). #1 (amber via Phaser/GameScene) — GameScene.ts has NO external-highlight store hook, BUT the project's cyan hint is a React DOM overlay (showHintOverlay; "showHint intentionally NOT called, GameScene stays untouched"). Owner approved (AskUserQuestion): render Rescue Flash as a React overlay (no GameScene change), and reconcile Parts 1-6 to the real code (gems == the existing settingsStore.hintCount balance; NO rename; the rewarded ad currently grants a free highlight, not a balance gem). PREMISE CORRECTIONS: Part 1 — no hints/gems split exists (single hintCount field shown as 💎); added an addGems() alias instead of renaming. Part 5 — rewardedAdService awards nothing today; the WATCH AD button grants a free highlight in GameScreen → added addGems(3) there. Part 3/4 — the app has ONE once-per-day daily CLAIM (all-3-done) + a manual 7-day-streak weekly CLAIM, not a per-challenge/%7 counter; mapped +3 to the daily claim and +7 to the weekly claim. **Changes:** preferences.ts (LAST_DAILY_REWARD_DATE); settingsStore.ts (lastDailyRewardDate + load, addGems(), claimDailyGems(today)→+3 once/UTC-day); gameStore.ts (rescueFlashActive/rescueTileIds/rescueBannerShown reset on every start*, markRescueBannerShown(), activateRescueFlash(n) via engine.getSequence()); rescueAdService.ts NEW (separate rewarded ad → activateRescueFlash; pure isRescueEligible()); HomeScreen.tsx (3rd "Gems" stat card + claimDailyGems on mount + "+3 💎" CSS float); DailyHubScreen.tsx (claim→addGems(3), weeklyClaim→addGems(7) with distinct gold-bg/navy-text toast); GameScreen.tsx (P5 addGems(3) on rewarded watch; P7 rescue banner below grid/above hint cards + amber tile overlays — #FF8C00 bg, white number, gold pulse, pointer-events:none so taps pass to Phaser; reuses the cyan-overlay grid geometry); IAPScreen.tsx (addGems(20)). i18n: 7 new keys (home.gems_label/daily_reward, daily.gems_awarded/weekly_bonus, game.rescue_5/4/3) + updated iap.hint_pack_title/desc + game.watch_ad_sub, all 6 locales = 250 keys, parity OK. Tests: settingsStore.test.ts +6 (claimDailyGems once/day + idempotent + addGems amounts 3/7/20), new rescueAdService.test.ts +8 (isRescueEligible all conditions incl. 3×3/≤15s/<3 tiles/removeAds/bannerShown/threshold/timed). 219 → 233/233 green, tsc clean. DEVICE VERIFICATION (SM-E146B): ✓ daily gems — Home shows the 💎 gem card; relaunch same day = no double award; prefs lastDailyRewardDate=2026-06-09 confirms the +3 grant fired. ✓ 3×3 exclusion — L1 at 7s (timer running) shows NO rescue banner. ✓ rescue banner — on a 4×4 (Speed L10, 30s) at TIMER 6, the gold-bordered navy banner "⏱ Low on time? Watch ad → reveal 5 tiles" renders below the grid, above the hint cards. ✓ removeAds exclusion (condition 5) — with removeAdsPurchased=true the banner is correctly suppressed (required flipping the pref to false via run-as prefs injection to show it; DEV-002 removed the jump/reset tools). ✓ Part-5 label — WATCH AD card now reads "+3 gems free". CONSTRAINTS (not fully device-verified, covered by unit tests + the proven cyan-overlay pattern): the amber reveal and the +3-gems-on-watch award are gated on a FILLED live rewarded ad — the rescue ad no-filled in test (no inventory/warm-up), so no reward → no amber; L19 (the brief's example) is locked with no jump tool. One-per-attempt: the banner dismissed on tap and did not reappear. Device removeAds restored to true (baseline). Awaiting push approval. |

| 09 Jun 2026 | F-005-FIX: Rescue banner timer sync + removeAds scope + home card layout | Three device-found F-005 issues. **FIX 1 (timer sync) — PREMISE CORRECTION:** the brief unlocked GameScene.ts to write the timer to the store, but the countdown is NOT in GameScene — it's the React TimerComponent (GameScene has no timer/tick). So GameScene.ts was LEFT UNTOUCHED; instead gameStore gained `timeRemaining` + `setTimeRemaining`, reset to the level timeLimit in every start*, and GameScreen's TimerComponent.onTick now writes the authoritative `remaining` to the store; rescue reads gameStore.timeRemaining. (Note: F-005's `timeLimit−timeElapsed` was already mathematically equal to `remaining`, so the banner not appearing was actually FIX 2, not a timer drift — but the explicit field is cleaner and matches the brief's intent.) **FIX 2 (removeAds scope):** removed the `removeAdsPurchased` gate from isRescueEligible (param + condition) and the AC6 instant-grant shortcut in GameScreen.handleWatchAd — rewarded WATCH AD + rescue ads are opt-in and now available to ALL players (incl. Remove-Ads owners); removed the now-unused removeAdsPurchased selector. interstitialAdService.ts removeAds gate CONFIRMED still present (interstitials stay suppressed). **FIX 3:** HomeScreen 3 stat cards now flex:1 (equal width). **FIX 4 i18n:** grep of changed files → 0 hardcoded strings (only a comment matched). Tests: rescueAdService.test #11 updated ('eligibility IGNORES Remove Ads' → true) + OK baseline dropped removeAds; new gameStore.test.ts +2 (timeRemaining resets to timeLimit on startLevel; setTimeRemaining updates). 233 → 235/235 green, tsc clean. GameScene.ts UNTOUCHED (git diff empty). DEVICE VERIFICATION (SM-E146B, install -r over the existing removeAdsPurchased=true paid-user state — the exact condition the headline fix targets): ✓ FIX 3 — Home Best Score/Day Streak/Gems all measure 113px (equal). ✓ FIX 2/#3 — on Speed 4×4 at TIMER 7 with removeAdsPurchased=TRUE the rescue banner "⏱ Low on time? Watch ad → reveal 5 tiles" APPEARS (was suppressed before the fix). ✓ FIX 4/#4 — WATCH AD card visible+present for the paid user. ✓ FIX 1 — banner fires at the 33% threshold (explicit timeRemaining store field). NOT re-verified (ad-gated, live no-fill; unit-tested): the amber reveal after watching the rescue ad (#2) — the live rewarded ad no-fills in test, and L22 stays locked (no jump tool). Both F-005 (26e2b66) and F-005-FIX are LOCAL ONLY — awaiting push approval; to be pushed together. |

| 09 Jun 2026 | F-006: UI polish — rescue banner, home pills, daily gems copy, HowToPlay step 5 | Six visual/copy changes (no logic). **C1 rescue banner (GameScreen.tsx):** single pill → two-line — gold bold "⏱ LOW ON TIME" hero (constant) + white "Watch ad → reveal N tiles" sub (N=min(5,remaining)); orange box-shadow glow keyframe (bright #FF8C00 → pale rgba(255,140,0,0.4), 0.6s). i18n: added game.rescue_heading + rescue_sub_5/4/3; removed game.rescue_5/4/3. **C2 Home pill 1 (HomeScreen.tsx):** the EXPERT achievement card → daily-claim info pill — cyan SOLID border (no dot/circle), "💎 CLAIM 3 DAILY GEMS" gold + "Visit every day…" cyan sub, no navigation. **C3 Home pill 2:** the PRO achievement card → split PRO|EXPERT pill — gold solid border (radius 24), 60%-height centre divider, ⚡ PRO (purple) "Level 100" left + ★ EXPERT (cyan) "Level 200" right, both flex:1 → /campaign. Removed the now-unused AchievementCard component + tier/proCompleted/expertCompleted state + loadLocalTier/Tier imports. New keys home.daily_pill_title/sub + pro/expert_pill_title/sub (PRO/EXPERT/Level 100/Level 200 universal). (Left the orphaned home.win_* keys in place — harmless, parity preserved.) **C4 Daily (DailyHubScreen.tsx):** reward card daily.gem_reward_label "+1 GEM"→"+3 GEMS"; weekly copy daily.streak_goal "...claim 3 💎 gems"→"7" (the actual visible string — NOT complete_7) + daily.claim_weekly "WEEKLY 3"→"7" + hardcoded "✓ WEEKLY 3 💎"→"7" (all 6 locales). **C5 HowToPlay (HowToPlayScreen.tsx):** added step5 to the page-2 step array (['step3','step4','step5']; numbered circle "5", same card style) + how_to_play.step5 (6 locales, amber rescue instruction). i18n parity: all 6 = 258 keys. 235/235 tests green (copy-only, no new tests), tsc clean. DEVICE VERIFICATION (SM-E146B): ✓ C1 two-line banner with gold hero + white sub + orange glow (Speed 4×4, TIMER 7). ✓ C2/C3 Home pills redesigned — pill 1 cyan-solid "💎 CLAIM 3 DAILY GEMS"/cyan sub no dot; pill 2 gold-solid split ⚡PRO Level 100 | ★EXPERT Level 200 with centre divider, no dot. ✓ C4 daily reward card "+3 GEMS" and weekly heading "Reach 7 days in a row to claim 7 💎 gems." ✓ C5 HowToPlay page 2 shows steps 3/4/5 with the amber rescue instruction at circle 5. Stacks on F-005-FIX (a627c21). All three (F-005 26e2b66, F-005-FIX a627c21, F-006) LOCAL only — awaiting push approval, to be pushed together. |

| 09 Jun 2026 | F-007: Pill shapes, dotted borders, LeaderPanel i18n, banner spacing | Four device-QA visual/copy fixes (no logic). **FIX 1 (GameScreen.tsx):** rescue banner → pill (borderRadius 8→50), padding 10×20, 80% width (−20%, device-QA refinement), centred horizontally (margin '8px auto') and vertically (8px top/bottom) in the gap between grid and the 3 hint cards (was full-width, marginBottom-only). **FIX 2 (HomeScreen.tsx):** both home pills → dashed + pill — pill 1 (daily claim) borderRadius 999→50, border '1px solid #00f5ff'→'1.5px dashed #00f5ff'; pill 2 (PRO/EXPERT split) borderRadius 24→50, '1px solid #FFD700'→'1.5px dashed #FFD700'. **FIX 3 (LeaderPanel.tsx — unlocked for i18n only):** useTranslation/t already present; wired the 4 hardcoded labels → t('leader.you'/'leader'/'score'/'time') (YOU div, LEADER div, both Score StatRows, both Time StatRows). New `leader` i18n section in all 6 locales (de uses ANFÜHRER, not FÜHRER, per brief). Updated LeaderPanel.test.tsx #12/#13 (t mocked to return the key → assert 'leader.time—'/'leader.time6s' instead of 'Time—'/'Time6s'). **FIX 4:** PRO/EXPERT pill t() wiring CONFIRMED already wired (F-006 used t('home.pro_pill_title') etc.) — no change. i18n parity: all 6 = 262 keys. 235/235 tests green, tsc clean. DEVICE VERIFICATION (SM-E146B, pm clear): ✓ FIX 1 — rescue banner pill (computed radius 50px, marginTop==marginBottom==8px), centred, two-line, orange glow (Speed 4×4). ✓ FIX 2 — both home pills dashed + radius 50 (computed: cyan rgb(0,245,255) + gold rgb(255,215,0), borderStyle dashed). ✓ FIX 3 — switched to KO: in-game compact panel AND result screen show 나/리더/점수/시간; verified via DOM + screenshots. ✓ FIX 4 — PRO/EXPERT pill renders in KO (tier names universal). Device language restored to EN. NOTE: orphaned home.win_* keys left in place (per owner, harmless). Awaiting push approval (stacks on F-006 c6fdcac). |

| 10 Jun 2026 | F-009: Two-pill rescue redesign (GET A CLUE + LOW ON TIME + 1-use/game) | Replaced the single F-005/F-006/F-007 rescue banner with two side-by-side flex:1 pills below the grid. LEFT = GET A CLUE (cyan #00f5ff, dashed 1.5px, radius 50): visible at timeRemaining ≤ floor(timeLimit*0.6667) AND tilesRemaining≥3 AND gridSize>3 AND timeLimit>15 AND !cluePillUsed AND timed; tap → showClueAd(min(5,tilesRemaining)) reveals amber tiles + markCluePillUsed; greyed (faded border/heading, "Already used", pointerEvents:none) after use. RIGHT = LOW ON TIME (gold #FFD700): visible at ≤ floor(timeLimit*0.3333) (NEVER rendered before its threshold) AND gridSize>3 AND timeLimit>15 AND !timePillUsed AND timed; tap → showTimeExtensionAd() (+15s setTimeRemaining(current+15) + markTimePillUsed), mirrors +15s onto the visible TimerComponent (bonusSeconds prop) and floats "+15s" (gold, time-ext-float keyframe, 1.5s) over the HUD timer. WATCH AD gem card now 1-use/attempt: greyed + "Ad used" (watch_ad_used) + pointerEvents:none once gemAdUsed (set via markGemAdUsed after reward). gameStore: cluePillUsed/timePillUsed/gemAdUsed (+ 3 mark* actions), reset in startLevel/startFreePlay/startDailyChallenge/resetGame. rescueAdService: isRescueEligible→isClueEligible (66.66% threshold, `used` param), showRescueAd→showClueAd. NEW timeExtensionAdService.ts (isTimeExtensionEligible 33.33%, showTimeExtensionAd, TIME_EXTENSION_SECONDS=15). i18n: ADDED clue_pill_title, clue_pill_sub_3/4/5, time_pill_title, time_pill_sub, pill_used, watch_ad_used, time_extended across all 6 locales (31 game keys each, parity, content spot-checked); REMOVED rescue_heading + rescue_sub_3/4/5. Tests: rescueAdService.test rewritten for isClueEligible (8), new timeExtensionAdService.test (7), +gameStore flag/extension tests; 242→252/252 green, tsc clean (tsconfig.app.json 0 errors). Stacks on F-008 (21dff7a). Device verification PENDING internal-testing-track build (live rewarded ads no-fill in test). Awaiting B-023 then joint push (F-008+F-009+B-023). |

| 10 Jun 2026 | B-023: Player UID system (NTxxxxxx permanent identity) | Permanent auto-generated player identity replacing alias+country as true leaderboard identity. settingsStore: generatePlayerUid() ("NT"+6 from [A-Z0-9], 8 chars), playerUid field, loaded from Preferences (PREF_KEYS.PLAYER_UID) on first hydrate and generated+persisted if absent — never regenerated; no setter. preferences.ts: PLAYER_UID key. player_uid stamped on every score insert: campaignScores.submitCampaignScore, dailyScores.submitDailyScore, and supabase.submitScore (scores table) — the last reads playerUid from the store directly so gameStore.ts (LOCKED this brief) stays untouched. Leaderboard RPCs return player_uid: CampaignLeaderRow/AllTimeLeaderRow + DailyLeaderRow gained playerUid (daily select adds player_uid). LeaderboardScreen: UID column (Space Mono 10px, muted cyan rgba(0,245,255,0.6); brighter 0.95 for own row) between rank and flag in list rows AND the pinned YOUR-RANKING row; player-row match switched from alias to playerUid (alias fallback for pre-UID rows). NEW aliasService.migrateAliasInSupabase(uid,newAlias): one UPDATE per table (campaign_scores/daily_scores/scores) filtered by player_uid — re-labels ALL historical rows instantly; fire-and-forget, swallows errors; no-ops on empty uid. SettingsScreen: PLAYER IDENTITY section (UID gold Space Mono 16px + 🔒 + permanent sub-text) above PROFILE; alias input onBlur triggers migration. UID NOT added to LeaderPanel (GameScreen/ResultScreen) per Part 6. i18n: settings.player_identity_title/uid_label/uid_permanent across 6 locales (20 settings keys each, parity, content spot-checked). DEVIATIONS (documented): (1) supabase.ts edited — not in brief unlock list but NOT locked; required because the leaderboard fetch RPCs + row types live there (player_uid display impossible otherwise). (2) gameStore.ts (submitScore caller) is LOCKED this brief, so playerUid is read inside submitScore rather than threaded through the caller. Supabase Part 0 schema CONFIRMED applied by Mahendra (columns+indexes+RPCs). Tests: +5 settingsStore (UID format/length/charset/persist/no-regen), +3 aliasService (player_uid in insert, migrate-3-tables, empty-uid no-op), +1 LeaderPanel (UID never rendered); 252→261/261 green, tsc clean. Stacks on F-009 (f986435). Device verification PENDING (pm clear → UID gen, persistence, leaderboard column, no-UID-in-gameplay, alias migration). Awaiting joint push (F-008+F-009+B-023). |

| 10 Jun 2026 | VER-003: versionCode 6 / versionName 2.4 + rescue pill rule updates | build.gradle: versionCode 5→6, versionName "2.3"→"2.4". Both rescue pills tightened: isClueEligible (rescueAdService) + isTimeExtensionEligible (timeExtensionAdService) gridSize >3 → >=5 (4×4 now excluded; 5×5 is the floor) and timeLimit >15 → >=45 (sub-45s levels excluded). LOW ON TIME reward TIME_EXTENSION_SECONDS 15→30 (+30s). i18n: game.time_pill_sub + game.time_extended → +30s across all 6 locales (ko uses +30초; en/de/fr/pt/es +30s); dist spot-check confirmed 9×"+30s" + 2×"+30초", zero stale "+15s". DEVIATION (owner-approved via AskUserQuestion): GameScreen.tsx was LOCKED by this brief but had to be unlocked for a one-line correctness fix — handleTimeTap mirrored the reward onto the visible TimerComponent via a hardcoded setTimeBonus(b => b + 15); since TimerComponent.onTick overwrites the store's timeRemaining every tick, the service's setTimeRemaining(+30) was transient and the player would still have visibly received +15s. Fixed to setTimeBonus(b => b + TIME_EXTENSION_SECONDS) (imported the constant) so the visible +30s can never drift from the service value again. Tests: rescueAdService.test + timeExtensionAdService.test updated (removed the old ≤15s tests; added VER-003 boundaries — 4×4 false, 5×5 true, 44s false, 45s true; reward asserts +30). 261→267/267 green, tsc 0 errors. Build: clean + bundleRelease + assembleRelease BUILD SUCCESSFUL; aapt2 badging app-release.apk → versionCode='6' versionName='2.4'; AAB 8.55 MB signed (keystore.properties). Committed + pushed directly (version bump + small rule change, no main-thread gate per brief). Stacks on B-023 (a3f7b16). |

| 10 Jun 2026 | F-010: Manual resume after ads + time pill copy fix | Replaced F-008's 3-2-1 auto-resume countdown with a player-driven RESUME GAME overlay (the countdown could resume while the player was still on the ad end-card or a browser redirect). gameStore: removed resumeCountdown field, startResumeCountdown action, and the module-level resumeIntervalId/clearResumeInterval helper; added adJustCompleted:boolean (default false) + setAdJustCompleted(value) action; resumeTimer now just un-pauses + clears the flag; adJustCompleted reset in startLevel/startFreePlay/startDailyChallenge/resetGame. GameScreen: new full-screen overlay (rgba(7,17,31,0.97), zIndex 10000) shown when adJustCompleted — "Game is paused" (white Space Mono 14px) + gold "▶ RESUME GAME" button (navy text, 8px radius, 14×32 padding) that calls setAdJustCompleted(false)+resumeTimer(); tapping outside the button does nothing; old countdown overlay JSX + resumeCountdown selector removed; phaser-container pointerEvents now gated on adJustCompleted. TimerComponent.getPaused() per-tick check RETAINED (Samsung WebView background-timer guard — explicitly kept per brief). DEVIATION (documented, clear-technical-fix): brief said set setAdJustCompleted in the 3 ad services, but startResumeCountdown was actually called from the GameScreen handlers (handleWatchAd/handleClueTap/handleTimeTap) — the services only pauseTimer. Placed setAdJustCompleted(true) at those same handler points (replacing startResumeCountdown) because that path runs for ALL outcomes incl. no-fill 'unavailable' (which pauses but never shows an ad); putting the flag only inside a service's shown-path would leave a no-fill stuck paused with no overlay. pauseTimer on ad start unchanged in all three services. i18n: added game.ad_completed_pause + game.resume_game (6 locales), removed game.resuming_in, updated game.time_pill_sub → "Watch ad → Add +30s Time" equivalents (6 locales, 32 game keys each, parity, content spot-checked). Tests: replaced the 5 F-008 resume-countdown tests with 6 F-010 adJustCompleted tests (default false, set true stays paused, reset on startLevel, resume clears flag, pause/resume, no auto-resume); 267→268/268 green, tsc 0 errors. NOTE: overlay-render assertion (brief test items 5/6) covered via the store flag + mandatory device screenshots rather than a jsdom render — GameScreen boots Phaser and has never been unit-rendered. Stacks on VER-003 (f7fcad6). Device verification PENDING (overlay after gem/clue/time ads, timer frozen, manual resume, 10s no-auto-resume, browser-redirect safety). NOT pushed — awaiting report validation. |

| 10 Jun 2026 | B-024: Interstitial reset on rewarded ad + footer layout fix | FIX 1: watching ANY rewarded ad to completion now defers the post-game interstitial. interstitialAdService gained exported resetInterstitialCounter() — sets COUNT_KEY('levels_since_last_ad')=0 AND CAP_KEY('last_interstitial_shown')=Date.now() (restarts the 3-min clock); identical to the reset a successful interstitial show performs, so showInterstitial() was DRY'd to call it. Called (void, fire-and-forget) ONLY on confirmed reward in all three services: rewardedAdService.showRewarded (inside if(rewarded), gem +3), rescueAdService.showClueAd (after activateRescueFlash + markCluePillUsed, GET A CLUE), timeExtensionAdService.showTimeExtensionAd (after setTimeRemaining +30 + markTimePillUsed, LOW ON TIME). NOT called on dismiss/no-reward/no-fill (guarded by the same if(result)/if(rewarded) that gates the reward). No import cycle (interstitialAdService imports none of the three rewarded services). Counter model unchanged: dual trigger (timeElapsed≥3min OR completions≥5), both persisted in Preferences. FIX 2: post-interstitial the WebView could leave the fixed BottomNav off-screen (viewport not recalculated; previously needed 2 back-presses). ResultScreen's mount effect now fires setTimeout(()=>window.dispatchEvent(new Event('resize')),150) after `await maybeShowInterstitial()` — forces the WebView to re-lay-out fixed/absolute elements so BottomNav returns; harmless when no ad showed; 150ms lets the ad overlay finish dismissing. LOCKED BottomNav.tsx untouched (fix lives entirely in the dismissal handler). Tests: new interstitialReset.test.ts (4 — gem/clue/time reset on reward; dismiss does NOT reset) using a mocked AdMob bridge (Dismissed listener fired on next tick) + spied resetInterstitialCounter; 268→272/272 green, tsc 0 errors. Stacks on VER-004 (4244c1d). Device verification PENDING (game-4 WATCH AD → game-5 no interstitial → 4 more → fires on reset; footer visible after interstitial). NOT pushed — awaiting report validation. |

| 10 Jun 2026 | F-011: ResultScreen focus-trap fix + scrollable content | INVESTIGATION: GameScene.ts (locked) does NOT release canvas focus or set input.enabled=false on game end — confirms root cause B (nothing frees the canvas focus). GameScreen navigates to /result 600ms after status→complete/failed (then unmounts, Phaser destroyed). ResultScreen root was a fixed 100vh flex-column with overflow:hidden; the breakdown container already had height:auto+flexShrink:0, so Bug A's clipping came from the WHOLE column overflowing on small/medium screens (6 breakdown rows + stars + score + LeaderPanel + promo + buttons + BottomNav > 100vh, clipped by overflow:hidden) — NOT a single fixed height. FIX 1 (back button): new exported setGameCanvasInteractive(enabled) in ResultScreen.tsx (Phaser-free module so tests import it without Phaser) — blur()+pointerEvents:none on false, pointerEvents:auto on true; GameScreen calls it (false) in the complete/failed effect (blur from React; GameScene stays locked); ResultScreen handleNextLevel/handlePlayAgain call it (true) for the next game. DEVIATION (clear-technical-fix, documented): brief's document.querySelector('canvas') would hit GameScreen's decorative #particle-canvas (first in DOM, already pointerEvents:none), NOT Phaser's — corrected to querySelector('#phaser-container canvas') with a first-canvas fallback. Also the brief put the NEXT/PLAY-AGAIN restore "in GameScreen", but those handlers live in ResultScreen — placed the restore there (both files unlocked); GameScreen imports the helper from ResultScreen (no cycle; ResultScreen is Phaser-free). FIX 2 (clipping): wrapped header→stars→score→breakdown→LeaderPanel→GazeticaPromoCard in a scroll region (flex:1, overflowY:auto, WebkitOverflowScrolling:touch, paddingBottom:16); action buttons container lost marginTop:auto, gained flexShrink:0 so NEXT LEVEL/PLAY AGAIN + BottomNav stay outside the scroll, always visible; bg-dots/ParticleCanvas remain full-screen background siblings. Tests: +4 in ResultScreen.test.tsx (release→pointerEvents none; release→blur frees focused canvas; NEXT LEVEL→auto via real click; PLAY AGAIN→auto via real click); 272→276/276 green, tsc 0 errors. NOTE on Bug B: GameScreen unmounts on /result navigation so the blur fires in the 600ms pre-nav window; if device QA shows back still dead, the deeper cause may be that ResultScreen registers NO Android back handler at all (GameScreen's is removed on unmount) — flag for a follow-up if needed (out of this brief's stated scope). Stacks on VER-005 (a401990). Device verification PENDING (Level 33 all 6 rows visible both devices; back button responds on ResultScreen). NOT pushed — awaiting report validation. |
